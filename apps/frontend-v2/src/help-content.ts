const HELP: Record<string, { title: string; content: string }> = {
  releaseNotes: {
    title: "Release Notes - 版本发布说明",
    content: `> 每次版本发布后,本文档会在顶部增量追加最新版本的更新内容,历史版本依次往下保留。
> 想看具体功能怎么用,请到左侧对应模块的帮助页;想知道"最近改了啥"看这里就够。

## v2.5.0 — 2026-05-31 (旗舰特性:Hermes Tool-using Agent + 14 通用工具)

本版把 Hermes 从「intent 路由器」升级为「Tool-using Agent」——LLM 自己看 schema 选工具,不再硬编码 intent 正则。v2.4 现网"有多少员工"这类问题(intent 命不中 → fallback-search 空)v2.5 直接命中 \`count_nodes\` 返回精确答案。

### 四桶整合

**① 通用工具集** (\`apps/backend/src/hermes-tools.ts\`,1033 行)
- **14 个只读工具**: list_node_types / describe_node_type / count_nodes / query_nodes / get_node / search_text / traverse_graph / get_progress / get_audit / aggregate / dashboard_metric / recommend_helpers / ticket_tabs / welink_*
- **统一 filter DSL**: 支持 \`eq/ne/gt/gte/lt/lte/in/like\` + 简写等值;key 正则 \`/^[A-Za-z0-9_一-鿿]+$/\` 兼容中文拒注入;所有 SQL 参数化
- **私单收口**: attackTicket 类工具全部经 \`filterAccessibleTickets\`;用户 A 私单对用户 B 不可见
- **32KB 出参截断**: 数组二分截尾,对象置错;\`_truncated:true\` 透传给 LLM 提示翻页
- **写工具默认禁用**: \`HERMES_ENABLE_WRITE=1\` 扩展点
- HTTP: \`GET /api/hermes/tools\` + \`POST /api/hermes/tool/:name\`;CLI: \`hermes:tools\` / \`hermes:tool\`

**② Tool-using Agent** (\`apps/backend/src/hermes-agent.ts\` 改造)
- OpenAI-compatible tool-calling 协议;LLM 看 \`TOOL_SCHEMAS\` 自决调用
- 多轮编排: 默认 \`MAX_TOOL_HOPS=6\`;单工具出参 \`TOOL_RESULT_MAX_BYTES=32KB\`;累积 \`CONTEXT_MAX_BYTES=80KB\` 触发上下文折叠(早期 tool result 压缩成 summary)
- 三模式: \`HERMES_MODE=auto|tool|intent\`,\`auto\` 短问题(<30 字符 + 命中 intent 正则)走 intent 快路径,其余走 tool
- 失败 fallback: tool 超 hop/超时/LLM 错 → 自动回退到 intent 路由,trace 标 \`fallback_reason\`

**③ 前端 trace UI** (\`apps/frontend-v2/src/components/ToolTrace.tsx\`)
- 答复气泡内可展开「🔧 工具调用 (N 步,共 Xms)」时间线
- 每步显示工具名 + 入参 size + 出参 size (\`_truncated\` 橙色徽标) + 耗时(按阈值分级染色)
- \`engine\` badge: 蓝色"工具调用" / 灰色"规则路由";\`fallback_reason\` 黄色 Alert
- 优雅降级: 旧 response 无 trace 时完全不渲染,与 v2.4.1 行为一致

**④ 文档 + 评测**
- \`docs/HERMES_TOOLS.md\` 完整文档
- 15 题 golden set e2e 评测,门槛 12/15

### 测试

- Backend **649/649** 通过(90 文件 = v2.4 baseline 575 + tools 57 + agent 17)
- Shared **28/28** 通过
- Frontend trace UI e2e **4/4** + 抖动回归 **1/1** 通过
- Frontend tsc 0 错

### 经验沉淀

> 把硬编码 intent 升级为通用工具集后,新需求(新报表、新指标、新过滤)不再要求"加一个 endpoint + 加一个 intent 正则";LLM 自己组合工具回答。代价是延迟变高(多轮 LLM + 工具往返),所以 \`auto\` 模式保留 intent 快路径处理高频简单问句。这是 v2.6+ Agent 能力进一步扩张的基座(memory / planner / reflection)。

## v2.4.1 — 2026-05-31 (Hot-fix)

线上紧急修复,仅含两条修补,无新特性。

### 修复

- **攻关单详情打不开(React #310)**: \`apps/frontend-v2/src/pages/AttackDetail.tsx\` 中 \`useAttackDetailHandlers\` hook 调用位置位于 \`if (data.initialLoading || !data.node) return <Spin/>\` 之后,触发 React Hooks 调用顺序违规 → ErrorBoundary 兜底"页面出了点问题/重试"。修法:把 hook 调用搬到 early return 之前,destructure / 派生值 / early return 全部后移。
- **HermesChat 思考时滚动条上下抖动**(bug \`de1bf88e\`,/contributions 等所有挂载 AI 助手的页面):autoscroll \`useEffect\` 把 \`loading\` 列入依赖且无视用户手动上滑;新增 \`stickToBottomRef\` + \`onScroll\` 仅贴底时自动滚 + \`requestAnimationFrame\` 包裹 + 容器 CSS \`overflow-anchor: none; contain: layout\`。

### 验证

- Playwright 现网 e2e 校验 \`/attack/<id>\` 完整渲染、0 console error
- 新增回归测试 \`e2e/hermes-chat-no-oscillation.spec.ts\`
- backend tests 575/575 / shared 28/28 / frontend tsc 0 错

### 经验沉淀

> Hooks 规则不可妥协:任何 hook(useState/useEffect/自定义 hook)必须在组件 render 路径的所有 early return 之前调用。重构 hook 抽取(把 useState 收到子 hook)时尤其要复核调用顺序——子 hook 内部新增/删除 useState 等价于父组件的 hooks 数量变化,一旦在 conditional 分支下调用就会触发 #310。

## v2.4.0 — 2026-05-31 (三桶 — 现网加固 + 架构韧性 + 升级 UI 真实化)

本版聚焦"长期可运营":修末位 CVE、把 KG 派生改为可重放 outbox、给审计加完整性链、让升级 UI 能拉 GitHub Release 并校验 PGP 签名,真正具备生产可用性。

### 三桶整合

**① 现网加固 P2**
- **exceljs 替换 xlsx**: 末位 CVE 清零;Excel 导入导出能力等价
- **PM2 cluster 多进程**: 新增 \`ecosystem.config.cjs\`,生产可启用 cluster 模式(Postgres adapter 推荐)
- **编译 dist 去 tsx**: 后端生产从 \`tsx src/server.ts\` 改为 \`node dist/server.js\`,冷启动更快、内存更稳
- **异地备份脚本**: 新增 \`backup:offsite\` CLI + \`scripts/backup/offsite-backup.mjs\`,支持 SFTP 远端备份(含 dry-run)
- **Sentry 错误聚合**: 前后端集成 \`@sentry/node\` + \`@sentry/react\`,DSN 未设时完全 no-op,详见 \`docs/OBSERVABILITY.md\`

**② 架构韧性**
- **KG outbox + worker**: KG 派生从 \`setImmediate\` 改为持久化 outbox 表 + 后台 worker,进程崩溃可重放;\`kg:outbox:status/list/replay/process\` 四个 CLI
- **audit_log Merkle 链**: 每条审计记录带 \`prevHash/hash\`,任意篡改可被 \`audit:verify\` CLI 精确定位
- **前端 catch 全迁移**: 70+ 处 \`catch (e: any)\` 统一为 \`handleApiError\` + \`ApiError\`,错误码/详情可见

**③ 升级 UI 真实化**
- **GitHub Release 拉取**: 在线列出 Release 并直接选版本升级(env \`UPGRADE_GITHUB_REPO\`)
- **PGP 签名校验**: 升级包可附 \`.asc\` 签名;analyze 阶段自动校验,失败禁用 apply(可勾"允许未签名"二次确认放行)
- **生产真跑演练**: \`scripts/upgrade/prod-rehearsal.mjs\` 支持 \`--dry-run\`(可达性 + 版本快照 + 打包) 与 \`--apply\`(真升级 + 自动 rollback)
- **UPGRADE 文档补真实演练流程 + 失败恢复 SSH 介入步骤**

### 关键文件 / 新增 CLI

- \`apps/backend/src/kg-outbox.ts\` / \`audit-chain.ts\` / \`pgp.ts\` / \`xlsx-util.ts\`
- \`scripts/backup/offsite-backup.mjs\` / \`scripts/upgrade/prod-rehearsal.mjs\` / \`ecosystem.config.cjs\`
- 新 CLI: \`audit:verify\`、\`kg:outbox:*\` (4)、\`backup:offsite\`、\`upgrade:releases\`、\`upgrade:verify-signature\`

### 测试

- Backend: **573/573 通过**(85 文件)
- Shared: **28/28 通过**
- Frontend-v2: typecheck 0 错

## v2.3.0 — 2026-05-31 (旗舰特性:一键升级 UI + v2.2 P1 三桶继承)

本版在 v2.2.0(sec/perf/quality 三桶 P1)基础上,叠加 **一键升级 UI** 旗舰特性 — 用户可在浏览器内完成版本升级、Schema 自动合并、失败自动回滚。

### 一键升级 UI(旗舰特性)
新增「系统管理 → 系统升级」(仅 admin),支持上传 .tar.gz 升级包,自动分析 diff、备份、回滚的全流程编排。

#### 核心机制
- **Schema overlay 系统**:用户在 UI 加的字段写到 \`data/schemas-overlay/\`,跨升级保留;baseline (\`config/schemas/\`) 随代码包整盘替换
- **三方 schema 合并**(current_baseline + current_overlay + target_baseline):用户字段名撞新基线 → 列入冲突报告供决策
- **自我升级 orchestrator**:detached Node worker 跑 backup → extract → schema-merge → secrets → code-swap → restart → health,任一步失败自动回滚
- **MVP 限制**:本地上传包(不支持 GitHub release 拉取/PGP 签名校验,留 v2.4);自我升级生产化需在测试环境真跑 systemd 重启一次再上线

#### 后端
- 8 端点 \`/api/upgrade/*\`(current/upload/analyze/apply/status/rollback/history/log),走 v2.2 新增的 \`adminMiddleware\` 守卫
- worker.mjs detached 进程(stdio: ignore + unref),状态文件 \`data/upgrade-state.json\`,历史 \`data/upgrade-history.json\`
- 兼容 mock-systemd 模式(\`COMBAT_UPGRADE_MOCK_SYSTEMD=1\`),本机/e2e 跳过 systemctl

#### 前端
- 三段式 SystemUpgrade 页面:① 上传 → ② diff 报告 Drawer → ③ 双重确认(Checkbox + 输入 UPGRADE)→ 执行
- 升级中 Progress + Steps + 实时 log tail(轮询 /api/upgrade/status 1.5s)
- 完成后展示历史 Table + 回滚按钮
- api.ts 8 个 upgrade 方法走 v2.2 新增的 \`ApiError\` 类型化错误,401 自动跳登录

#### 测试
- 后端 \`upgrade-router.e2e.test.ts\` 18 用例 + \`schema-overlay.unit.test.ts\` 11 用例
- 前端 \`system-upgrade.spec.ts\` 4 用例(本地 e2e)

### v2.2.0 P1 三桶继承
v2.3.0 完整继承 v2.2.0 所有 P1 改造,无回退:

- 🔒 **安全**:helmet 响应头 / 全局 + 登录 rate-limit / CSRF 同源 Referer / 私密单全集过滤 / SMTP AES-256-GCM / 强制首登改密 / multer 2.x + express 4.21 / audit actor 强制 req.user
- ⚡ **性能**:queryNodesByProperty SQL 下推 / conflicts 30s 防抖 + 增量 / recommend 消除 N+1 / proposer 长度差预筛 / appendProgress 原子 seqNo / Prometheus /api/metrics
- 🏗️ **质量**:AttackDetail 1823→327 行(拆 6 组件 + 2 hook + 1 builder)/ 前端 vitest 54 用例 / ApiError 类型化 / makeTestApp 单源

### 文档体系
- \`docs/UPGRADE.md\` — 一键升级流程、回滚、故障排查
- \`docs/SECURITY_RUNBOOK.md\` — 安全运营 / 事故响应(v2.2 沿用)
- \`docs/PERFORMANCE_TUNING.md\` — 性能调优手册(v2.2 沿用)
- \`docs/REVIEWS/REVIEW_*.md\` — 5 专家评审 + 各桶"v2.2 P1 已实施"段

### 部署
- 后端 vitest **~536 全绿**(v2.2 baseline 507 + upgrade-ui 29:overlay 11 + router 18)
- 前端 vitest **54/54**(v2.2 quality 继承)
- 三端 \`tsc --noEmit\` 全 0 错
- 部署同 v2.2:\`cd scripts/deploy-v2 && node deploy-direct.mjs 124.156.193.122 root <pass>\`

---

## v2.2.0 — 2026-05-31 (Roadmap P1 三桶整合 — 安全/性能/质量)

继 v2.1.0 的 P0 清零后,本版处理 5 位专家评审中所有的 **P1 重要项**,分 3 桶并行落地:**7 P1 安全 + 7 P1 性能 + 4 P1 质量**。各桶在独立分支验证后整合入 master。

### 🔒 安全(7 P1 全清)
- **helmet 安全响应头**:CSP / HSTS / X-Frame-Options / X-Content-Type-Options 等浏览器侧防御一次性开
- **全局 + 登录 rate-limit**:全局 1000 req/15min/IP,登录 5 次/15min/IP,挡暴破和爬虫
- **CSRF 同源 Referer 校验**:写操作强校验 \`Referer\` / \`Origin\` 同源,挡跨站伪造
- **私密攻关单"全集过滤"**:list/audit/export/dashboard 等任何返回 attackTicket 的入口都走 \`filterAccessibleTickets\`,防越权读元信息
- **SMTP 凭证 AES-256-GCM 加密**:数据库静态字段加密 + 启动期 \`SMTP_ENC_KEY\` 校验
- **默认 admin/admin123 强制首登改密**:登录后端返回 \`passwordMustChange\`,前端 ForcePasswordChange 全屏拦截
- **multer 1.x → 2.x + express ≥4.21.2**:消除 multer/express 已知 CVE
- **audit log actor 强制 req.user.username**:不再信任 body.actor / 硬编码 "api",新增 \`actorOf(req)\` helper 统一收口

### ⚡ 性能(7 P1 全清 + 可观测性)
- **queryNodesByProperty SQL 下推**:json_extract + 表达式索引,emailGroup/person 等值查找从全表扫降为索引点查;消除 routes/dashboard 多处 N×M 内存过滤
- **conflicts 30s 防抖 + 增量算法**:窗口内单 ticket 多次保存合并为 1 次 \`syncConflictsForOne\`,>50 ticket 兜底降级到全量,audit 写放大降 10-100×
- **recommend 推荐找帮手消除 N+1**:历史贡献按 personId 批量预聚合,N+1 query 降为 2 次
- **proposer Levenshtein 长度差预筛**:|len(a)-len(b)| > threshold 直接 reject,N² → ~N log N 估
- **appendProgress 原子 seqNo**:UNIQUE(targetId,seqNo) + WAL 内事务,防竞态丢序
- **EXPLAIN + benchmark 实测**:scripts/bench-queryNodes.mjs + bench-explain.mjs 给出实测口径
- **Prometheus /api/metrics 端点**:Histogram(http 时延)+ Counter(请求数 by status)+ Node.js 默认 process 指标,公开免鉴权,供采集器拉

### 🏗️ 代码质量(4 P1)
- **AttackDetail 1823 行 → 327 行**:拆 6 子组件(Header/Sidebar/4 个 Tab/Drawers) + 2 hook(useAttackDetailData/Handlers) + 1 builder(buildTabItems)
- **frontend vitest 单测起手**:7 文件 / 54 用例 / 100% 通过 — utils(auditFilter/handleApiError/nodeLabel/teamMembers) + hooks(useSettings) + components(StatusTag) + api(ApiError)
- **ApiError 类型化 + 401 自动跳登录**:消除前端 100+ 处 \`(e: any)\`,handleApiError helper 统一收口
- **makeTestApp 单源**:去除 3 处副本,所有 backend test 共用同一 helper

### ✅ 测试覆盖(整合分支验证)
- 后端 vitest: **507/507 全绿**(基线 463 + sec 23 + perf 14 + quality 7 = 44 新增)
- 前端 vitest: **54/54 全绿**(全新增)
- TypeScript: backend + frontend-v2 + shared 三端 \`tsc --noEmit\` 全 0 错

### ⚠️ 整合策略说明
- 按依赖顺序合:perf(基础数据通路) → quality(重构 + 测试) → sec(收尾守卫)
- 冲突解决原则:**特性 union**(perf 的 nodeId 参数 + sec 的 actorOf actor,quality 的 handleApiError + sec 的 passwordMustChange 同时保留)
- \`private-tickets.ts\` 由 sec 桶从 routes.ts inline 拆出来,再套上 perf 的 \`queryNodesByProperty\` SQL 下推
- master 当前 8b3f223(v2.1.0),与三桶共同祖先一致,无需 master 二次合

---

## v2.1.0 — 2026-05-31 (Roadmap 4 桶整合 — 安全/性能/UX/质量)

依 5 位专家评审产出的 5 P0 安全 + 5 性能 quick wins + 5 UX 改进 + 5 代码质量,四桶并行落地后整合入 master。**全部为本机改动,所有桶均在合并前独立 push 并 CI 通过**。

### 🔒 安全(P0 全清)
- **JWT_SECRET 生产环境必填**:启动期校验,缺失或等于默认值直接退出进程(防默认弱密钥)
- **gradeGate 从 JWT 取 role**:不再信任 \`X-Role\` HTTP 头(localStorage 写入 + curl 可伪造),前端去掉所有 \`X-Role\` 发送
- **/auth/register 强制 role=normal**:公开自注册接口强制规整角色,提权必须由已登录 admin 创建
- **adminMiddleware + leaderMiddleware 统一守卫**:audit/merge/op-logs/backup/proposals/reminders/email 全部限 admin;tickets/:id/tabs + documents 写操作限 leader+
- **DynamicCustomTab / ManualCenter 关 rehypeRaw**:防 markdown 注入存储型 XSS

### ⚡ 性能(5 项 quick wins)
- **useSettings 真缓存**(module-level singleton + 5min TTL + 并发去重):13 个 callsite 重复 fetch → 1 次/5min,首屏少 12 个请求
- **GET /api/health**:无需鉴权,返回 \`{status,uptime,version,db:{kind,connected}}\`,systemd/反代/监控可探活
- **conflicts.syncConflicts 30s 防抖**:连续 10 次保存 = 1 次全量重建,audit 写放大降低 10-100×
- **backend.log 自动 rotation**:防爆盘,部署脚本 logrotate 自动注册
- **dashboard 单次扫表**:5 次 attackTicket 全表扫 → 1 次内存聚合;top-5 用末位替换法省一次 N·logN 排序

### 🎨 UX(全站 5 项升级)
- **AI 助手浮窗挂到全站 AppLayout**:所有页面右下角可问 AI(原仅攻关详情可见)
- **Dashboard 以我为中心**:首屏增加「我的攻关」「我的提醒」「我的草稿」三块卡片,跨页跳转直达
- **AttackList 批量操作**:多选 + 批量删除 + 批量加关注;列表底部固定 Action Bar
- **Cmd+K 命令面板**:全局快捷跳转(各列表/详情) + 新建(攻关单/贡献/求助等)
- **菜单瘦身**:文档/搜索/KG/问题反馈/帮助中心收纳到「工具」分组;系统管理保留管理类入口

### 🏗️ 代码质量(基础设施)
- **ESLint + Prettier 仓库根配置**:统一规则,husky pre-commit + lint-staged 自动 fix
- **GitHub Actions CI**:backend + shared 测试 + tsc 全过(双端)
- **Husky pre-commit hook**:lint-staged 拦截不合规提交
- **Repository Row 类型化**:消除 14 处 \`as any\`(注:postgres async 整合后,DbAdapter 路径采用 unknown 泛型替代)
- **README 新人 5 分钟 quick start + 必读文档清单**

### ✅ 测试覆盖
- 后端 vitest **463/463**(原 348 + welink 21 + health 2 + RBAC 5 + 各 router async 覆盖)
- 前端 e2e 在整合分支再次验证(各桶独立分支已分别 401+ 全绿)

### ⚠️ 整合策略说明
- 4 桶依赖顺序合(quality 基础设施 → performance → security → ux),冲突均按"两边都要"原则手工合
- 再合 master(welink + postgres + UI 配置化):repository.ts 选择 master 的 async DbAdapter(因 postgres 必须异步,quality 的 Row 类型化让位)
- app.ts/auth.ts/routes.ts/dashboard.ts/conflicts.ts 全部为 async + 安全守卫 + 性能优化 的 union
- AppLayout 菜单为 UX 的「工具」分组 + master 的 \`/db-migration\` 的 union

---

## v2.0.0 — 2026-05-30 (Welink + Postgres + UI 配置化大整合)

本版是「welink 群消息集成」「postgres 双驱动」「UI 配置化」三大主线 + 5 位专家评审 + 技术博客的 release。

### 🔗 Welink 群消息集成(6 场景完整闭环)
- 详见下方 v1.2.0 段。**主干合入完整 21/21 e2e**。

### 🐘 Postgres 支持(脚手架 + 一键迁移 UI)
- 详见下方 v1.1.0-pg 段。**SQLite 仍是默认**;Phase 1-4 全部完成,Repository 已 async 化、JSONB+GIN 索引、SQLite/PG 测试各 430/430。

### 🎛️ UI 配置化(2026-05-30 主干新增)
- 5 个高优先级硬编码选项迁配置中心:
  - \`bug.severity.options\` —— 问题反馈严重程度(严重/较高/一般/建议)
  - \`bug.status.options\` —— 问题反馈状态
  - \`feedback.category.options\` —— 求助反馈分类
  - \`proposal.status.options\` / \`reminder.status.options\` —— 关系审批 / 跟催状态
  - \`team.role.options\` —— 团队角色枚举
- 新铁律写入 AGENTS.md:**所有 UI 文案/选项/枚举,不在源码硬编码,统一通过 \`useSettings.getValues/getOptions\` 读配置中心**
- \`useSettings\` API 升级:\`getValues(key, fallback)\` / \`getOptions(key, fallback)\` 支持 fallback,保证配置缺失时 UI 不空白

### 👥 人员合并移入系统管理
- 「人员合并」菜单从「人员与荣誉」组移到「系统管理」组,**仅 admin 可见可访问**;后端 \`/api/merge/*\` 加管理员守卫

### 📚 文档 & 评审产出
- \`docs/REVIEWS/\`:5 位专家(架构 / 性能并发 HA / 安全 / Staff Engineer 代码质量 / 现网运维)独立评审报告
- \`docs/BLOG_技术评审_作战平台.md\`:5 评审整合为深度技术博客
- \`docs/POSTGRES_SUPPORT.md\` / \`docs/REQUIREMENTS_WELINK.md\`:落地参考文档

### ✅ 测试覆盖
- 后端 vitest **SQLite 430/430 + Postgres 430/430**
- 前端 e2e **407/407**(含 welink 模块 21/21)

---

## v1.2.0 — 2026-05-30 (Welink 群消息全套闭环)

围绕「攻关单 ↔ Welink 群消息」打造完整工作流,场景 1/2/3/4 + 聊天视图 + 下载工具引导 闭环:

### 📥 下载工具引导(本期收尾)
- **Welink Tab 顶部置顶 Alert**:首次进入提示「📥 第一步:同步群消息」,内嵌 Windows 端下载工具直链(后端 \`GET /api/documents/:id/download\`)
- 工具由管理员预先上传到 **文档中心**,Tab 自动按 title 含「Welink + 下载/工具」识别最新版本;未上传时 Alert 变橙色「⚠️ 管理员还未上传」+「跳到文档中心」按钮
- Alert 用户可一键关闭,localStorage 持久化「已收到提示」,刷新不再弹

### 🗂️ 场景 1:消息归档与筛选
- 拖拽 / 点击上传 Welink 导出 JSON;按 messageId 覆盖式去重
- 列表视图:发言人/时段/关键词(包含+排除) 四维筛选,全选/反选当前过滤集
- 支持单条删除 / 批量软删 / 物理清空全部

### 💬 场景 2:聊天视图(沉浸式回看)
- 顶部 Segmented 切换「列表视图 / 聊天视图」,本机记忆偏好
- 时间序气泡布局,头像按发言人 hash 上色;TEXT / CARD / PICTURE 三类不同样式
- 引用块、@提及、http 链接自动高亮;按天分隔条;连续 5 分钟同人合并
- 兼容 Welink 原始字段(msgId / sender / serverSendTime / contentType + images 数组)

### 🤖 场景 3:AI 抽取 + 5 类结构化
- 「让 AI 分析」按钮触发 Hermes agent 抽取(失败回退启发式),把已选子集压成 5 类:**人物 / 时间线 / 决策 / 争议 / 缺口**
- 右侧 Drawer 分类展示;**「缺口」「人物」类一键「加入攻关成员」**,自动写回成员列表 + 三字段双向同步
- 右下「AI 助手 · 群消息补齐」浮窗:对话式问答 + 补齐成员(如「把活跃的人都加进来」「除张三外其他都加进来」)

### 🔗 场景 4:Hermes 回答里的群消息引用 + 跳转高亮
- 在攻关详情或 KG 浮窗问 AI「谁最早提到 OOM」「小王几号介入」等,AI 回答里出现 **「群消息」蓝色 Tag**(citation kind=welink,后端 \`hermes_search\`/\`hermes_timeline\` 工具返回 messageId)
- 点击 Tag → 自动跳转到对应攻关详情的 Welink Tab → 强制切「聊天视图」→ 滚动到目标消息并 **2 秒黄色高亮**(URL 参数 \`?welinkMsg=<id>\` 透传)

### ✅ 测试覆盖
- 后端 vitest **377/377**(welink 模块新增 30+ 测试覆盖 upload/list/select/analyze/extractions/citations)
- 前端 e2e welink 模块 **21/21**(tab 5 / chat-view 5 / extraction 5 / citation 3 / download-prompt 3)

---

## v1.1.0-pg — 2026-05-30 (Postgres 支持脚手架 + 一键迁移 UI)

\`feature/postgres-support\` 分支的增量。**SQLite 仍是默认,行为零变化**;新增 Postgres 双驱动基础设施 + 一键迁移工具。

### 🆕 数据库迁移 UI(系统管理)
- 新菜单项 **系统管理 → 数据库迁移**(仅管理员可见)
- 三段式 UI:① 当前驱动 + 各表行数 ② Postgres 连接表单 + 测试 ③ 试运行/正式迁移
- 后端调 \`scripts/migrate/sqlite-to-postgres.mjs\` 子进程,事务级一致

### 🆕 CLI 迁移工具
\`scripts/migrate/sqlite-to-postgres.mjs\`,支持 \`--dry-run\` / \`--truncate\` / \`--batch N\`;按表批量 INSERT、事务级提交、写迁移标记文件。

### 🆕 后端 DB_URL 配置(默认仍 SQLite)
- 新环境变量 \`DB_URL\`:支持 \`sqlite://./path\` / \`postgres://...\` / \`postgresql://...\` / 裸路径
- 启动期自动识别协议,建表;Postgres 路径会打印 \`server.postgres_phase1\` 警告(因为业务层 Repository 仍未 async 化)

### ⚠ 当前限制
- 一键迁移 UI 是脚手架,**真正切换到 Postgres 需要 Phase 2 (Repository 全面 async 化) 落地**
- Phase 2 体量大(~80 处 callsite),作为专项 sprint 单独排期
- 详见 \`docs/POSTGRES_SUPPORT.md\` 路线图

---

## v1.0.0 — 2026-05-30 (首版完整功能盘点)

作为第一版 release notes,本版**完整盘点**当前系统所有可用功能(包括本期新增 + 此前已上线的所有特性)。后续版本只追加增量。

### 🏆 本期核心新增

#### 攻关单私密协作
- 创建人在详情顶部可点 **设置私密**;私密状态下仅 创建人 + 成员 + 指定授权人/授权群组 可访问
- 列表 / 详情标题前显示 🔒 提醒(私密攻关单)
- 授权对话框: 下拉多选 **指定授权人员**(可搜索)+ 下拉多选 **指定授权邮件群组**(群组成员邮箱反查到的人均可访问)
- 创建人随时可 **管理私密授权** / **取消私密**;其他人完全不可见这两个按钮
- 后端 GET API 同步守卫,非授权用户直接访问 URL 返回 403,前端显示「无权访问」页

#### 攻关单成员多选 + 「成员管理」固定 Tab
- 攻关成员录入从输入框改为 **多选**,从全员名单搜索
- 新增固定 Tab **成员管理**: 表格显示 姓名 / 角色 / 操作; 支持增删改; 角色枚举 \`组长\` / \`组员\`
- 所有改动 **三字段双向同步**: 成员列表(JSON,结构化含角色) ↔ 攻关组长 ↔ 攻关成员

#### 攻关单创建后自动「信息广场」标签页
- 攻关单创建成功后自动挂一个 **信息广场** 自定义 Markdown 标签页

#### 攻关单删除权限收紧
- 后端创建时自动注入「创建人」= 当前登录用户
- 列表 / 详情删除按钮 **仅创建人本人可见**;管理员 / Leader 也看不到
- 后端 DELETE API 同步加守卫,防越权

#### 详情布局重构
- **面板默认收起**: 主内容占满 24 列,用户主动勾选才显示侧边卡
- **「进展同步」Timeline 合并**: progress + 过滤后关键审计(🟢 状态流转 / 🟠 升级 / 🟡 合并 / 🔵 成员变更)
- **删除「历史记录」固定 Tab**(内容并入进展 Timeline)
- **新增「合规追溯」侧边卡**(Leader+Admin 可见,Admin 多 3 条关键事件预览)→ 跳转审计日志自动过滤
- **基础信息字段隐藏/恢复**: Tab 顶部「字段管理」Popover 勾选;按用户名 localStorage 持久化

#### 攻关首页「我的关注」
- 顶部 Tab: \`全部\` / \`我的关注 (N)\`
- 列表第一列 ★: 点击关注/取消关注;按用户名隔离
- 已关注的行在「全部」Tab 下整行 **淡黄色高亮 + 左侧黄边** 提醒

#### 返回保留筛选
- 攻关单详情页 **返回列表** 会保留进入前的搜索词/筛选/Tab

#### 菜单与权限差异化
- **「审核管理」** 移入 **系统管理 → 审核管理**,仅管理员可见可访问
- AdminGuard 路由守卫:非 admin 用户直接访问 \`/proposals\` / \`/reminders\` URL 会跳回首页

#### 知识图谱 (@antv/g6 v5)
- 全新可视化页面 \`/kg\`
- **5 项浏览增强**: 节点详情侧栏 / 邻居高亮(hover) / 边类型筛选 / 布局切换+小地图+适应画布 / 节点定位
- **节点折叠/展开** 切换
- **浮动 AI 问答**:从 Drawer 改为 **可拖拽悬浮窗**

#### AI 助手 (Hermes)
- 攻关单详情 AI 助手对话框 **可鼠标拖拽**
- AI 助手把 **当前攻关单作为 context** 传给 agent,一步取详情
- agent 可读攻关单 **笔记 MD 内容**,综合作答 + 来源引用

#### 灵活 Excel 导入
- 未匹配列在预览阶段显示,可勾选「自动建字段」一键创建并继续导入

#### 审计与追溯
- **AuditLog 支持 URL \`?entityId=\` 自动过滤**
- **「实体」列显示语义化名称**(按 ID 反查),不再暴露内部 UUID
- 全站统一不向用户暴露数据库 UUID

#### 问题反馈 (截图反馈)
- 全站底部 **悬浮「截图反馈」按钮**
- 截图区支持 **拖拽上传 + Ctrl+V 粘贴**(替代原生文件框,部分受控浏览器禁了文件对话框也能用)
- 截图自动 JPEG 0.7 压缩 + body 上限 20MB

---

### 📦 已稳定运行的全部功能(本版承袭)

#### 攻关管理
- **攻关作战台**:列表 / 筛选 / 搜索 / 关注 / 导出 / 列拖拽 / 自定义可见列(localStorage 持久化)
- **攻关详情**:状态步骤条 / 摘要卡 / 基础信息 / 进展同步 / 日报更新 / 求助网络 / 关联全景 / 找帮手推荐 / 编辑信息 / 状态流转
- **攻关日报**:按日期/类型筛选;支持创建草稿、编辑、发布(已发布锁定);团队全量日报视图
- **动态标签**:支持自定义笔记(Markdown,可搜索高亮)+ 关联数据(按 nodeType 过滤)+ 全局广场海报(多选 infoCard);标签可重排序、删除
- **状态流转**:待响应 → 处理中 → 进行中 → 已解决 → 已关闭,按生命周期顺序
- **追加进展**:每条带状态快照,Timeline 展示
- **升级规则 / CCB 提醒 / 跟催提醒**:自动扫描超时单创建提醒

#### 人员与荣誉
- **全员名单**:CRUD + 部门 / 工号 / 邮箱 / 角色字段;导入导出
- **贡献录入**:贡献人 / 贡献类型 / 贡献等级 / 关联攻关单 / 周期;贡献等级标定仅 Leader+Admin 可写
- **团队贡献**:团队名称 / 贡献类型 / 贡献等级 / 描述 / 组长(单选) / 组员(多选) / 关联攻关单 / 周期
- **荣誉殿堂**:个人排行(按 贡献分 / 处理量 / 解决率)+ 团队荣誉(按贡献等级分组卡片 + 详情)
- **个人荣誉**:某人的贡献历史 / 协助记录 / 涉及攻关单清单
- **人员合并**:同名/同邮箱候选 → 选定主记录 → 合并(不可逆,审计追溯)

#### 协作工具
- **求助中心**:发起求助 / 设置目标人/邮箱/分类 / 反馈链接(公开访问,无需登录) / 反馈状态
- **文档中心**:文档上传(拖拽+原生 input 双通道) / Markdown 渲染 / 下载链接公开(无需 Bearer token)
- **信息广场**(Dashboard tab):全局信息卡片流;创建/编辑(标题+摘要+内容+重要程度+分类+发布人);过滤+搜索
- **知识图谱**(详见上方核心新增)

#### 审核与提醒(仅管理员)
- **关系审批**:扫描候选关系 → 人工审批通过/拒绝 → 落地为正式 edge
- **跟催提醒**:扫描后自动创建提醒;按状态分类(待发送/已发送/已忽略)

#### 检索与关联
- **全局搜索**:跨节点类型模糊查询;按 nodeType 过滤;命中字段高亮
- **关联全景**:某节点的所有关联(REF 引用 / 共享锚点 / 相似性建议);分类展示

#### 系统管理
- **表结构管理 (SchemaWizard)**:可视化字段管理;新增字段 / 标记弃用 / 修改 label/type/enum;删除影响分析
- **配置中心**:统一管理下拉枚举值(状态/事件级别/求助分类/...);optionsKey 绑定;DynamicField 组件;删除影响分析弹窗
- **邮件设置**:SMTP 配置 + 测试发送
- **邮件群组**:组名 + 成员邮箱(逗号分隔);用于通知 + 私密授权
- **数据导入/导出**:按 nodeType 批量;**灵活导入**(未匹配列自动建字段)
- **审计日志**(详见上方核心新增)
- **备份恢复**:立即备份 / 定时备份(每天/每周/每月,保留份数)/ 下载 / 恢复(上传 .db)
- **用户管理**(仅 admin):CRUD 用户 / 重置密码 / 角色(admin / leader / normal)
- **操作追踪**(仅 admin):前端所有 API 调用 / 路由导航 / 全局错误的会话级日志
- **系统升级**(仅 admin):一键升级 UI,上传 .tar.gz → 自动 diff → 双重确认 → backup/swap/restart/health 全流程编排
- **问题反馈**(详见上方核心新增)

#### 平台基础
- **JWT 鉴权**:登录 / 注册 / 修改密码;7 天 token;\`COMBAT_NO_AUTH=1\` 环境变量可关闭鉴权(开发/测试)
- **角色权限**:admin / leader / normal;贡献等级写入 / 私密管理 / 用户管理 / 审核管理 等差异化
- **CLI**(命令行):后端所有 HTTP API 均有对应 CLI 命令;\`npm run cli -- help\` 查看
- **AGENTS.md / CLAUDE.md**:开发者文档 + 测试状态追踪

---

## 后续版本

后续每次发布将在本文档 **顶部** 追加新版本块,历史版本依次保留;格式:

\`\`\`
## vX.Y.Z — YYYY-MM-DD
- 简述本次发布重点
### 分类
- 新功能 / 改进 / 修复 / Breaking 等
\`\`\`
`,
  },
  dashboard: {
    title: "作战态势 - 使用帮助",
    content: `## 作战态势面板

### 功能概述
作战态势是系统首页，以一屏展示当前攻关作战的整体态势。顶部四个统计卡片提供关键指标概览，中部柱状图呈现状态分布，底部最近动态列表实时追踪变化，帮助指挥者在 10 秒内掌握全局。

### 顶部 Tab 切换
- **总览**（默认）：上述统计卡片+柱状图+动态
- **信息广场**：全局信息卡片流（公告/记录），支持过滤+搜索+创建+编辑；URL 带 \`?tab=square\` 直达

### 操作指南

**查看统计卡片**
页面顶部展示四个核心指标：
- **总攻关单**：系统中所有攻关单的累计总数
- **进行中**：状态为「处理中」或「进行中」的活跃攻关单数量（蓝色数字可点击）
- **已闭环**：状态为「已解决」或「已关闭」的攻关单数量
- **今日进展**：当日新增的进展条目数，反映团队当日工作产出

**查看状态分布**
中部柱状图按五个状态展示分布：待响应（金色）、处理中（蓝色）、进行中（青色）、已解决（绿色）、已关闭（灰色）。鼠标悬停可查看精确数量。

**浏览最近动态**
底部列表展示最近更新的攻关单，每条显示标题、当前状态标签、更新时间（如"2小时前"）。点击任意条目直接跳转至该攻关单的详情页。

**「以我为中心」三卡**(2026-05-31 新增)
四个 Statistic 之后的一行展示三张小卡片，让你登录后立刻看到与自己相关的任务:
- **分配给我**: 当前处理人 == 你 且 状态属于 进行中/处理中/待响应 的攻关单（最近 5 条）。点击「查看全部」会自动用「当前处理人」筛选跳到攻关作战台。
- **我的关注**: 你在 AttackList 用 ★ 标注的攻关单（按用户名隔离的本地存储，换账号互不串扰），最近 5 条。点击「查看全部」跳 \`/attack?tab=favorites\`。
- **SLA 风险**: 状态=进行中/处理中/待响应 且 创建时间超过 3 天的攻关单，按"越早越靠前"排序，提示「超 N 天」橙色标签。无超期时显示 ✓ 友好空态。

**快速跳转**
- 点击「进行中」统计卡片 → 跳转到攻关作战台，自动筛选出活跃状态的攻关单
- 点击右下角「查看全部」链接 → 跳转到完整的攻关作战台列表

### 实际场景示例
> 周一早会上，组长打开作战态势页面，看到「进行中」卡片显示 **7**，说明有 7 个攻关单仍处于活跃状态。柱状图显示「待响应」有 3 个，意味着周末新报的问题还没有人认领。点击「进行中」卡片进入列表，发现「华为云 ECS 异常重启」已处理 3 天仍为处理中，立即安排人员接手。

### 注意事项
- 统计数据在页面加载时获取，不会自动刷新。需要最新数据时请刷新页面
- 「今日进展」统计的是当日 00:00 至今新增的进展条目，非攻关单数量
- 最近动态最多展示 20 条记录，按更新时间倒序排列

### 快捷操作
- 直接点击「进行中」数字是最快的进入活跃攻关单的方式，无需手动筛选
- 最近动态列表中的条目可直接点击跳转，比先进入列表再搜索更高效`,
  },

  attackList: {
    title: "攻关作战台 - 使用帮助",
    content: `## 攻关作战台

### 功能概述
攻关作战台是系统核心页面，集中管理所有攻关单的全生命周期。支持创建、筛选、搜索、关注、导出等操作。表格展示编号、标题（含 🔒 私密图标提醒）、状态、处理人、问题单号、客户名称、更新时间和操作列。顶部 Tab 提供「全部」「我的关注 (N)」两个视图。

### 操作指南

**新建攻关单**
1. 点击右上角「新建攻关」按钮，右侧弹出创建抽屉
2. 填写表单字段：
   - **标题**（必填）：简明描述问题，如「华为云 ECS 实例异常重启」
   - **状态**：默认为「待响应」，可手动选择
   - **事件级别**：高/中/低
   - **客户名称**：关联的客户名称
   - **当前处理人**：从全员名单搜索选择（支持中文模糊搜索）
   - **攻关组长**：从全员名单搜索选择
   - **攻关成员**（新）：**多选**，从全员名单搜索（组员角色；组长在上方单选，不必在此重复）
   - 其他字段：问题单号、事件单号等
3. 如需添加系统预设以外的字段，请到「系统管理 → 表结构管理」配置
4. 点击顶部「创建」按钮提交
5. **创建成功后**：系统自动跳转到详情页，并自动创建一个「信息广场」自定义标签页供团队记录

**我的关注**
- 顶部 Tab：\`全部\` / \`我的关注 (N)\`
- 列表第一列 ★：点击关注/取消关注（按用户名隔离，换账号互不串扰）
- 已关注的行在「全部」Tab 下显示 **淡黄色高亮 + 左侧黄边** 提醒

**筛选攻关单**
- **状态筛选**：点击「状态」下拉框，选择待响应/处理中/进行中/已解决/已关闭，支持清空还原
- **关键词搜索**：在搜索框输入关键词，系统在标题、处理人姓名、问题单号中模糊匹配，实时过滤
- 筛选条件会写入 URL，进入详情后**返回列表会保留**这些筛选

**列表交互**
- 点击表格任意一行 → 跳转到该攻关单的详情页
- 标题前出现 🔒 → 这是一个**私密攻关单**（仅创建人/成员/授权人可访问）
- 点击行尾「删除」→ **仅创建人本人可见**；其他人显示占位符 \`—\`
- 点击「导出」→ 将当前筛选结果导出为 xlsx 文件下载

**批量操作**(2026-05-31 新增)
- 每行前的复选框 / 表头「全选」可一次选中多行
- 选中 ≥1 行后,Tabs 下方出现蓝色工具条:
  - **批量加关注 ★**: 一次把选中的所有攻关单加入「我的关注」
  - **批量删除(仅创建人)**: 循环删除选中的攻关单;非本人创建的会**静默跳过**(不报错、提示中标注跳过数量),避免越权
  - **取消选择**: 清空选中状态
- 点选 checkbox 不会触发整行跳转

**全局快捷键 Cmd+K**(2026-05-31 新增)
- 按 \`Ctrl + K\`(Windows) / \`Cmd + K\`(Mac) 任何页面调出命令面板
- 输入页面名(如「攻关」「人员」「图谱」)直接跳;输入「新建」可触发「新建攻关单」直接打开抽屉
- 输入任意关键词后 Enter,跳转 \`/search?q=xxx\` 全局搜索

### 实际场景示例
> 客户报告新问题后，值班人员点击「新建攻关」，标题填写「阿里云 RDS 主备切换失败」，事件级别选「高」，客户名称输入「阿里巴巴」，攻关组长选「张三」，攻关成员多选「李四、王五」。提交后跳转详情页，发现已自动创建「信息广场」标签页。点击 ★ 关注该单，后续在「我的关注」Tab 中能快速定位。

### 注意事项
- 标题是唯一必填字段，但建议尽量填写处理人和成员，便于后续筛选与排查
- 人员选择下拉使用中文模糊搜索，输入拼音首字母不会匹配，请输入中文姓名
- 删除按钮只有创建人本人能看到（管理员/Leader 也不行）；老数据无创建人则任何人都看不到，需走 CLI/数据库清理
- 导出的是当前筛选条件下的数据，如需全量导出请先清空筛选

### 快捷操作
- 在搜索框输入客户名称可快速定位该客户的所有攻关单
- 关注重点攻关单后，在「我的关注」Tab 一屏看全自己关心的工作
- 表格列宽可拖拽调整，横向滚动查看更多字段`,
  },

  attackDetail: {
    title: "攻关详情 - 使用帮助",
    content: `## 攻关详情页

### 功能概述
攻关详情页是单个攻关单的全生命周期工作台。顶部标题区（含 🔒 私密标识）+ 操作按钮组；中部状态步骤条 + 摘要；下方固定 Tab + 动态 Tab；右侧可选侧边卡（默认收起，需手动勾选打开）。

### 顶部操作按钮（按角色和状态动态显示）
- **面板** — 控制右侧侧边卡的显示/隐藏（默认全部收起，让主内容占满 24 列）
- **关联全景** — 跨视图的完整关联关系图
- **设置私密 / 管理私密授权 / 取消私密** — **仅创建人本人可见**
  - 未私密时显示「设置私密」；私密后显示「管理私密授权」+「取消私密」两个按钮
  - 设置私密弹抽屉：下拉多选 **指定授权人员**（可搜索）+ 下拉多选 **指定授权邮件群组**
  - 私密生效后：列表 + 详情标题前显示 🔒；仅 创建人 + 成员 + 授权人/群组 可访问，他人收到「无权访问」提示
- **状态流转** — 选择目标状态 + 备注 → 写入审计日志
- **编辑信息** — 修改字段；攻关成员保持多选交互
- **删除** — **仅创建人本人可见**；其他人（包括管理员/Leader）完全不显示

### 固定标签页（6 个）

| 标签 | 内容 |
|------|------|
| 基础信息 | 字段表格 + 顶部「**字段管理**」Popover 勾选要显示的字段；偏好按用户名持久化 |
| **成员管理**（新） | 表格：姓名 / 角色 / 操作；支持新增、修改角色、删除；角色枚举 \`组长\` / \`组员\`；任何改动**三字段同步**（成员列表 ↔ 攻关组长 ↔ 攻关成员） |
| **Welink 消息**（新） | 上传群消息 JSON,选择/删除/送 AI 分析；拖拽 .json 上传 → 时段/发言人/关键词筛选 → 勾选纳入分析子集 → 「让 AI 分析」按钮**触发 AI 抽取**(优先调 Hermes agent,失败回退启发式)。**列表视图 / 聊天视图 双模式**:顶部 Segmented 切换;聊天视图按时间序气泡布局 — 头像按发言人 hash 上色、TEXT/CARD/PICTURE 三类样式、引用块、@提及与 http 链接自动高亮、按天分隔条、连续 5 分钟同人合并;支持解析 Welink 原始字段(msgId/sender/serverSendTime/contentType + images 数组)。**AI 抽取**结果分 5 类(人物/时间线/决策/争议/缺口)在右侧 Drawer 展示;**「缺口」「人物」类一键加入攻关成员**;右下「AI 助手 · 群消息补齐」浮窗按钮支持对话式问答 + 补齐成员(例:"把活跃的人都加进来"、"除张三外其他都加进来")。**Welink 消息可被 AI 回答引用**,问"谁最早提到 OOM/小王几号介入"等问题时回答里的「群消息」蓝色 Tag 点击会自动跳转到聊天视图并 2 秒黄色高亮原消息(场景 3)。**首次使用三步走**:① Tab 顶部「📥 第一步:同步群消息」Alert 点链接下载 Windows 端 Welink 消息下载工具(管理员预先上传到文档中心,Tab 自动从 \`/api/documents\` 查找含「Welink + 下载/工具」的最新条目);② 本机运行工具登录 Welink → 选群 → 导出 JSON;③ 回 Welink Tab 把 JSON 拖到上传区 → 切到「聊天视图」/「列表视图」核对 → 勾选纳入分析子集 → 「让 AI 分析」。**Alert 可关闭**(本机 localStorage 记忆,刷新不再弹);若管理员尚未上传工具,Alert 变橙色「⚠️ 管理员还未上传」并提供「跳到文档中心」按钮 |
| 进展同步 | Timeline 合并展示：原 progress + 过滤后关键审计（🟢 状态流转 / 🟠 升级 / 🟡 合并 / 🔵 成员变更）；Leader+Admin 可点「查看完整历史」跳转审计日志 |
| 日报更新 | 表格展示日报条目，支持创建/编辑/发布/删除（已发布的不可编辑） |
| 求助网络 | 树形展示支撑节点，支持模板应用 |

> 「历史记录」固定 Tab 已下线，关键事件并入「进展同步」Timeline；完整审计走右侧「合规追溯」卡跳转。

### 动态标签页
- 创建攻关单后系统自动挂一个 **「信息广场」** 自定义 Markdown 标签页
- 团队可继续点「添加标签」创建更多：自定义笔记（Markdown）或关联数据（按 nodeType 过滤显示）

### 右侧侧边卡（面板控制）
- **找帮手推荐** — 基于共享锚点和历史贡献智能推荐协助人员
- **合规追溯**（Leader / Admin 可见，Admin 多 3 条关键事件预览）— 点「查看完整历史 →」直跳 \`/audit?entityId=<本单>\` 自动过滤

### AI 助手（可拖拽）
- 自定义笔记标签里可点「AI 助手」展开浮窗
- 浮窗**可鼠标拖拽**（顶部条按住），不再卡在右下角
- 提问基于知识库 + 本攻关单笔记综合作答，返回可点击的来源引用

### 实际场景示例
> 「华为云 ECS 异常重启」攻关单中，组长进详情页：
> 1. 点「面板」勾选「合规追溯」，右侧出现关键事件预览
> 2. 进入「成员管理」Tab，添加 2 名组员
> 3. 在「进展同步」Tab 追加进展，Timeline 同时显示进展和「成员变更」蓝色 Tag
> 4. 客户反馈含商业敏感信息，组长点「设置私密」，加上 PM 张三 + 法务群组，列表标题前出现 🔒
> 5. 切到「信息广场」标签写一段决策记录给团队同步

### 注意事项
- 删除按钮只有创建人能看；管理员/Leader 也看不到；老数据无创建人则没人能从 UI 删
- 私密授权的「邮件群组」会展开成员邮箱 → 反查人员姓名 → 判定访问；群组改动后无需重新保存
- 面板每次进入都默认收起（不持久化）；字段隐藏偏好持久化（按用户名）
- 求助网络模板可在配置中心预设，重复使用提高效率
- 删除攻关单会级联删除所有进展、日报和求助节点，此操作不可逆

### 快捷操作
- 追加进展是最常用操作，进入详情页后直接切换到「进展同步」标签即可
- 历史记录标签可快速查看某人是否修改过关键字段，利用变更详情定位问题
- 求助网络支持模板一键应用，比逐个创建支撑节点更高效

### v2.4.1 修复
- 修复:打开攻关单详情页直接显示"页面出了点问题/重试"(React #310 hooks 调用顺序违规),原因为 \`useAttackDetailHandlers\` hook 调用位于 early return 之后,已搬到 hooks 之前固定调用顺序

### v2.6 新增
- **面包屑导航**:详情页顶部新增「首页 → 攻关管理 → 攻关作战台 → 攻关单 xxxxxxxx」面包屑,可点击逐级返回
- **逾期通知**:本攻关单若超出 SLA 触发升级,**当前处理人 + 创建人**会在「通知中心 (🔔)」收到一条 escalation 提醒;邮件失败或求助回复也会推到提报人收件箱
- **Schema 驱动详情页**:基础信息 Tab 与编辑抽屉全部由 \`schema.fields\` 驱动,按 \`group\` 自动分 Card / 分组 Divider 渲染,**新字段在 SchemaWizard 加好后详情页自动出现**,无需改前端代码
- **通用字段渲染器**:\`components/SchemaField.tsx\` 支持 string/number/boolean/enum/textarea/date/datetime/ref(person)/array/json 全类型;view 只读 / edit 受控
- **特殊字段保留专用 UI**:specialControl=member-multi/member-list/private-flag/private-grants/system 等字段保留各自 Tab/Drawer,不会被通用渲染器抢占
- 详细介绍:在 SchemaWizard 加字段 → 选好分组 → 立即在攻关单详情可见;再去看 \`docs/SCHEMA_AS_UI.md\``,
  },

  peopleList: {
    title: "全员名单 - 使用帮助",
    content: `## 全员名单

### 功能概述
全员名单管理系统中的所有人员信息，是其他模块（攻关单处理人、贡献人、求助人）的数据基础。支持单个添加和批量导入，提供部门筛选、关键词搜索、导出 Excel 等功能。每条记录包含姓名、工号、邮箱、部门、角色等字段。

### 操作指南

**添加人员**
1. 点击右上角「添加」按钮，右侧弹出创建抽屉
2. 填写表单：
   - **姓名**（必填）：人员真实姓名
   - **工号**：员工编号
   - **邮箱**：工作邮箱，用于求助中心发送邮件
   - **部门**：所属部门名称
   - **角色**：人员角色（normal/leader/admin）
   - 如需新增字段（如手机号、办公地点等），请到「表结构管理」选中对应表后添加，表单会自动显示
3. 点击顶部「添加」按钮提交

**批量导入**
1. 点击「导入名单」按钮，弹出导入抽屉
2. 先点击「下载模板」获取标准 Excel 模板文件
3. 按模板格式填写人员数据（表头必须与模板一致）
4. 将填好的 Excel 文件拖拽到上传区域或点击选择文件
5. 系统预览导入内容，显示将创建、更新和跳过的记录数
6. 确认无误后点击「确认导入」

**筛选与搜索**
- **部门筛选**：下拉选择部门，仅显示该部门人员
- **关键词搜索**：输入姓名、邮箱或工号进行模糊匹配，实时过滤

**列表操作**
- 点击行尾「编辑」→ 打开编辑抽屉，修改后点击「保存」
- 点击行尾「荣誉」→ 跳转到该人员的个人荣誉页面，查看贡献和积分
- 点击行尾「删除」→ 弹出确认对话框，确认后永久删除（关联的攻关单和贡献记录中的人员引用不受影响）

**导出**
点击「导出」按钮，将当前筛选结果下载为 xlsx 文件。

### 实际场景示例
> 新项目启动，需要录入 30 名团队成员。管理员点击「导入名单」→「下载模板」，在 Excel 中批量填写姓名、工号、邮箱、部门（如"云平台部"、"网络运维组"）等信息。上传后系统识别出 28 条新建、2 条更新（工号重复的已有人员），确认导入。随后在列表中按部门筛选「云平台部」，确认 12 名成员信息无误。

### 注意事项
- 姓名为唯一必填字段，但强烈建议填写工号和邮箱——工号用于去重判断（导入时的身份键），邮箱用于求助中心发送邮件
- 批量导入时，工号相同的记录会被识别为更新而非新建
- 删除人员不会级联删除其贡献记录和攻关单关联，但人员选择器中将不再显示该人
- 角色字段影响系统权限：normal 用户无法设置贡献等级（核心/关键/普通），leader 和 admin 可以

### 快捷操作
- 搜索框支持中文姓名部分匹配，输入「张」即可找到所有姓张的人员
- 导入前先导出当前数据，可快速了解现有人员的字段格式和数据结构
- 点击「荣誉」按钮可直接跳转查看某人的贡献统计，无需先去荣誉殿堂查找`,
  },

  contributions: {
    title: "贡献录入 - 使用帮助",
    content: `## 贡献录入

### 功能概述
贡献录入用于记录和管理团队成员在攻关过程中的各项贡献，是荣誉殿堂排名的数据来源。每条贡献包含贡献人、类型、等级、描述、关联攻关单和周期等字段。贡献等级按权重计算积分：核心=3分、关键=2分、普通=1分。

### 操作指南

页面分上下两张表：上方「个人贡献」、下方「团队贡献」。

**录入个人贡献**
1. 点击右上角「录入个人贡献」按钮，右侧弹出创建抽屉（480px）
2. 填写表单：
   - **贡献人**：从全员名单搜索选择，支持输入中文姓名模糊匹配
   - **贡献类型**：选择贡献类别
   - **贡献等级**：选择「核心」/「关键」/「普通」（**需要 Leader 或 Admin 角色**）
   - **描述**（Textarea）：详细描述贡献内容
   - **关联攻关单**：可选，关联到具体攻关单
   - **周期**（Input）：贡献所属时间段，如「2026年5月」
3. 点击顶部「提交」按钮保存

**录入团队贡献**
1. 点击右上角「录入团队贡献」按钮，弹出团队抽屉
2. 填写：团队名称、贡献类型、贡献等级（必填）、描述（必填）、**组长**（单选）、**组员**（从全员名单多选）、关联攻关单、周期
3. 提交后出现在下方「团队贡献」表；团队贡献会在荣誉殿堂的「团队荣誉」中按等级展示

**筛选与搜索**
- **等级筛选**：下拉选择核心/关键/普通，过滤特定等级的贡献
- **关键词搜索**：输入贡献人姓名进行模糊匹配

**列表交互**
- 点击贡献人姓名（蓝色链接）→ 跳转到该人的个人荣誉页面
- 点击行尾「编辑」→ 打开编辑抽屉修改信息
- 点击行尾「删除」→ 弹出确认对话框
- 点击「导出」→ 下载当前筛选结果为 xlsx 文件

### 实际场景示例
> 攻关单「华为云 ECS 异常重启」成功解决后，组长在贡献录入页面点击「录入贡献」。贡献人搜索选择「张三」，贡献类型选「问题定位」，贡献等级选「核心」（因为张三连续加班 3 天定位到根因），描述填写「独立完成内核 panic 根因分析，定位为 cgroup 内存限制触发 OOM」，关联攻关单选择「华为云 ECS 异常重启」，周期输入「2026年5月」。提交后，张三在荣誉殿堂的个人排行中获得 3 分（核心=3×1次）。同时在荣誉殿堂可看到排名更新。

### 注意事项
- **贡献等级需要 Leader 或 Admin 角色**：页面左上角角色切换器（默认为 normal）需切换为 leader 或 admin，否则选择贡献等级后提交会返回 **403 Forbidden** 错误
- 贡献人下拉显示格式为「姓名 (部门)」，便于区分同名人员
- 关联攻关单为可选项，但建议关联以便在详情页查看贡献上下文
- 删除贡献记录会实时影响荣誉殿堂的排名和积分

### 快捷操作
- 利用等级筛选快速查看所有「核心」级别贡献，评估团队高价值产出
- 点击贡献人姓名跳转比去荣誉殿堂搜索更快
- 先在攻关作战台确认攻关单标题，再到贡献录入关联，避免选错

### v2.4.1 修复
- 修复:嵌入页面的 AI 助手在思考阶段(Hermes 流式返回时)滚动条上下抖动(bug \`de1bf88e\`),用户上滑离开底部后不会再被强制拖回底部

### v2.6 新增
- **面包屑已上线**:列表页顶部新增「首页 → 人员与荣誉 → 贡献录入」面包屑,可点击逐级返回`,
  },

  honor: {
    title: "荣誉殿堂 - 使用帮助",
    content: `## 荣誉殿堂

### 功能概述
荣誉殿堂用于激励和认可成员与团队。提供「个人排行」和「团队荣誉」两个视图。前三名个人以奖牌标识（🥇🥈🥉），支持按周期筛选和数据导出。

### 操作指南

**切换视图**
页面顶部提供两个标签页：
- **个人排行**：按个人累计加权积分排名（核心=3分、关键=2分、普通=1分）
- **团队荣誉**：展示「团队贡献」记录，按贡献等级（核心/关键/普通）分组为卡片，单击卡片在右侧查看团队详情

**个人排行**
- 表格展示：排名、姓名、部门、各项贡献次数（核心/关键/普通）、总积分
- 前三名在排名列显示奖牌：🥇 第 1 名、🥈 第 2 名、🥉 第 3 名，其余显示数字序号
- 积分计算：核心贡献次数 × 3 + 关键贡献次数 × 2 + 普通贡献次数 × 1
- 点击人员姓名（蓝色链接）→ 跳转到该人的个人荣誉详情页，查看具体贡献列表

**团队荣誉**
- 来源于「贡献录入」页录入的团队贡献记录（含组长、组员）
- 按贡献等级分组展示卡片，单击卡片在右侧查看团队详情（类型/描述/组长/组员/关联攻关单等）

**周期筛选**
- 使用筛选下拉框选择特定周期（如「2026年5月」），查看该周期内的排名
- 清空筛选可查看全量排名

**导出数据**
点击「导出数据」按钮，将当前排行结果下载为 xlsx 文件。

### 实际场景示例
> 月度总结会上，组长打开荣誉殿堂切换到「个人排行」，周期筛选选「2026年5月」。排行榜显示：🥇 张三 15 分（核心 3 次 + 关键 2 次 + 普通 2 次 = 3×3+2×2+1×2=15），🥈 李四 11 分，🥉 王五 8 分。组长点击张三的姓名进入个人荣誉页面，查看其 3 次核心贡献分别是「华为云 ECS 异常重启根因定位」「阿里云 RDS 主备切换方案设计」「腾讯云 CDN 故障应急响应」，在会上逐一表扬。随后切换到「团队排行」发现云平台部总分最高，获得本月最佳团队。

### 注意事项
- 积分是实时计算的，在贡献录入页面新增或删除贡献后刷新即可看到排名变化
- 周期筛选依赖贡献录入时填写的「周期」字段，未填写周期的贡献不会出现在周期筛选中
- 团队荣誉来源于「贡献录入」页录入的团队贡献记录；如未显示请先在该页录入团队贡献
- 个人排行中同分情况下按更新时间排序

### 快捷操作
- 导出数据功能可用于月报、季度汇报等场景，直接将排行表嵌入报告
- 周期筛选配合导出，可以快速生成任一时间段的贡献统计
- 从个人排行点击进入个人荣誉页面，再点「返回荣誉殿堂」回到排行榜，浏览效率最高`,
  },

  personHonor: {
    title: "个人荣誉 - 使用帮助",
    content: `## 个人荣誉详情

### 功能概述
展示单人的全部贡献记录和关联攻关单，是荣誉殿堂个人排行的详情展开页。页面顶部显示人员姓名和累计总积分，下方分别列出该人员的所有贡献条目和关联的攻关单。

### 操作指南

**页面导航**
- 左上角「返回荣誉殿堂」按钮：返回排行榜页面
- 页面顶部展示人员姓名和总积分（如「张三 · 总积分：15」）

**贡献列表**
以表格形式展示该人员的所有贡献记录：
- **贡献类型**：贡献类别
- **贡献等级**：核心（红色）/ 关键（橙色）/ 普通（蓝色），带颜色标签
- **描述**：贡献内容详细说明
- **关联攻关单**：关联的攻关单标题（如有）
- **周期**：贡献所属时间段
- **时间**：贡献录入时间

**关联攻关单**
页面下方展示该人员参与过的攻关单列表，包含标题、状态、处理时间等信息。点击攻关单标题可跳转到对应的攻关详情页。

### 实际场景示例
> 月度评审中，组长在荣誉殿堂看到张三排名第 1，点击其姓名进入个人荣誉页面。页面显示「张三 · 总积分：15」。贡献列表中 3 条核心贡献分别是：①「华为云 ECS 异常重启」根因定位 ②「阿里云 RDS 主备切换」方案设计 ③「腾讯云 CDN 故障」应急响应。下方关联攻关单显示张三参与了 5 个攻关单，其中 3 个已解决、1 个进行中、1 个已关闭。组长据此在评审会上全面展示张三的攻关贡献。

### 注意事项
- 总积分按贡献等级加权计算（核心=3、关键=2、普通=1），与荣誉殿堂排行算法一致
- 贡献列表按时间倒序排列，最新的贡献排在最前
- 关联攻关单仅展示该人员作为处理人或组长参与的攻关单
- 删除贡献记录后需刷新页面，积分和列表会实时更新

### 快捷操作
- 从贡献列表的关联攻关单列可直接跳转详情，无需返回列表页搜索
- 使用浏览器的返回按钮（或 Alt+←）可快速回到荣誉殿堂，不必点击页面上的返回按钮`,
  },

  helpCenter: {
    title: "求助中心 - 使用帮助",
    content: `## 求助中心

### 功能概述
求助中心用于向团队内部成员或外部专家发起结构化求助请求。系统通过邮件将问题推送给求助对象，并自动生成带 Token 的反馈链接。对方提交反馈后，内容自动追加到关联攻关单的进展记录中，形成完整的求助闭环。

### 操作指南

**发起求助**
1. 点击页面右上角「发起求助」按钮，打开创建抽屉
2. **关联攻关单**（必填）：从下拉列表中搜索并选择需要求助的攻关单，下拉显示攻关单标题
3. **求助人**（必填）：从全员名单中选择发起求助的人员
4. **求助对象**（必填）：从全员名单中选择被求助的人
5. **求助对象邮箱**（必填）：输入求助对象的邮箱地址，系统会校验邮箱格式
6. **求助类型**（必填）：选择「环境」「领域专家」「团队协作」「资源」中的一种
7. **求助内容**（必填）：在文本框中详细描述需要帮助的问题
8. **附加说明**（选填）：补充背景信息、截图链接等
9. 点击抽屉顶部的「发送求助邮件」按钮提交

**筛选与查看**
- 使用状态筛选下拉框过滤「待回复」或「已回复」的求助记录
- 在关键词搜索框中输入内容摘要或求助对象姓名进行模糊搜索
- 点击表格中攻关单列的前8位编号链接，可跳转到对应的攻关单详情页

### 实际场景示例

> 攻关单「线上OOM问题排查」处理过程中，发现需要特定环境的配合复现。处理人张三在求助中心点击「发起求助」，关联该攻关单，求助对象选择环境运维负责人李四并填写其邮箱，求助类型选「环境」，求助内容填写「需要一台与生产环境相同配置的8核32G测试机器，用于复现OOM问题，数据包已上传到共享目录」。李四收到邮件后点击反馈链接，填写反馈内容「测试环境已准备就绪，IP: 10.0.1.50，有效期3天」，该反馈自动出现在攻关单的进展记录中。

### 注意事项
- 发送邮件前，需确保「邮件设置」页面已正确配置 SMTP 服务。若未配置，系统仅记录求助但不发送邮件
- 每条求助会生成唯一的 feedbackToken 和反馈链接，链接无需登录即可访问
- 求助状态在对方提交反馈后自动变为「已回复」

### 快捷操作
- 表格默认按时间倒序排列，最新的求助在最前面
- 通过状态筛选「待回复」可快速找到还未获得反馈的求助，及时跟催

### v2.6 更新
LLM 全部 UI 配置，无需后台手工部署。详见左侧「系统管理 → LLM 设置」（仅 admin 可见）。`,
  },

  helpFeedback: {
    title: "求助反馈 - 使用帮助",
    content: `## 求助反馈页

### 功能概述
这是一个无需登录的公开页面，求助对象通过邮件中的专属反馈链接访问此页，查看求助问题详情并提交反馈。提交后反馈内容自动同步到关联攻关单的进展记录中，求助状态变为「已回复」。

### 操作指南

**查看求助信息**
1. 通过邮件中的反馈链接（格式为 \`/help/feedback/<token>\`）打开页面
2. 页面顶部展示求助上下文：
   - **攻关单标题**：关联的攻关单名称
   - **求助人**：发起求助的人员姓名
   - **求助内容**：完整的求助问题描述

**提交反馈**
1. 在「反馈内容」文本框中填写您的建议、方案或回答（必填）
2. 在「姓名」输入框中可选填写您的姓名，便于求助人识别反馈来源
3. 点击「提交反馈」按钮
4. 页面显示「反馈已提交，感谢您的帮助！」成功提示

### 实际场景示例

> 环境工程师王五收到一封求助邮件：「张三就攻关单"线上OOM问题排查"向您发起求助，求助类型：环境，内容：需要一台与生产环境相同配置的8核32G测试机器……」王五点击邮件中的反馈链接，在打开的页面中看到完整求助信息，在反馈内容框中填写「测试环境已准备就绪，IP: 10.0.1.50，SSH密码已通过企业微信单独发送，有效期3天。建议优先检查堆外内存使用情况」，填写姓名「王五」，点击提交。张三在攻关单进展中立即看到这条反馈。

### 注意事项
- 反馈链接包含唯一 Token，每个链接对应一条特定的求助请求
- 如果页面显示「加载失败」，说明链接无效或已过期，请联系求助人重新发送
- 反馈提交后无法修改或撤回，请确认内容无误后再提交
- 此页面无需登录账号，可以直接访问

### 快捷操作
- 反馈内容支持多行文本，可以粘贴代码片段、日志片段或配置信息
- 建议填写姓名以便求助人后续跟进联系`,
  },

  search: {
    title: "全局搜索 - 使用帮助",
    content: `## 全局搜索

### 功能概述
全局搜索支持跨所有数据类型进行全文检索，覆盖攻关单、人员、贡献等全部节点。系统按匹配度评分排序结果，帮助您在大量数据中快速定位任意节点并查看其关联上下文。

### 操作指南

**执行搜索**
1. 在页面顶部的搜索输入框中输入关键词（如攻关单标题、人员姓名、贡献描述等）
2. 按回车键或点击搜索按钮触发搜索
3. 系统遍历所有节点类型的字段进行匹配，结果列表按匹配度从高到低排列
4. 每条结果显示：摘要文本、节点类型标签、匹配度分数

**类型筛选**
- 点击搜索框旁的类型下拉选择器，可限定搜索范围
- 可选类型：「攻关单」「人员」「贡献」，不选则搜索全部类型
- 筛选后仅展示对应类型的匹配结果

**查看结果**
- **点击攻关单结果**：直接跳转到该攻关单的详情页（/attack/:id）
- **点击人员或贡献结果**：在页面右侧展开上下文面板，展示该节点的详细信息和关联边（如该人员参与的所有攻关单、该贡献关联的攻关单等）
- 在上下文面板中点击节点名称可继续跳转

### 实际场景示例

> 指挥者想了解「张伟」参与的所有工作。在搜索框输入「张伟」，类型选择「人员」，搜索结果显示两条记录。点击「张伟（平台部）」的搜索结果，右侧上下文面板展示该人员的详细信息以及他被分配的3个攻关单（REF边）和5条贡献记录。点击其中一条贡献「定位并修复数据库死锁问题」，继续查看关联的攻关单详情。

### 注意事项
- 搜索为模糊匹配，输入部分关键词即可命中
- 搜索范围覆盖节点的所有字段（标题、描述、处理人、自定义字段等）
- 匹配度评分基于关键词出现频率和字段权重综合计算

### 快捷操作
- 清空搜索框可重置结果
- 搜索人员时输入部门名称也可命中，例如输入「平台部」会匹配到部门字段包含该值的全部人员
- 在上下文面板中浏览关联关系后，可点击浏览器后退按钮返回搜索结果`,
  },

  proposals: {
    title: "关系审批 - 使用帮助",
    content: `## 关系审批

> **菜单位置**：系统管理 → 审核管理 → 关系审批（**仅管理员可见可访问**；非 admin 直接访问 URL 会被路由守卫跳回首页）

### 功能概述
关系审批页管理系统自动发现的候选人员重复关系。扫描引擎基于名称相似度（Levenshtein 距离算法）识别可能是同一个人的多条记录，生成审批提案。审批者通过「通过」执行人员合并或「拒绝」排除误报，确保人员数据的唯一性和准确性。

### 操作指南

**扫描候选**
1. 点击页面右上角「扫描候选」按钮触发自动扫描
2. 系统遍历所有人员节点，两两比较名称相似度，生成候选提案
3. 扫描完成后表格自动刷新，展示新发现的候选记录

**审批操作**
- **通过**：点击操作列的「通过」按钮
  - 系统执行人员合并：被合并方的所有字段值合并到保留方（非空字段覆盖），所有关联边（REF、CONTRIBUTED_TO 等）迁移到保留方，被合并方被永久删除
  - 审计日志记录合并操作
- **拒绝**：点击操作列的「拒绝」按钮
  - 标记为非重复，后续扫描不再生成相同提案

**查看详情**
- 点击表格中「来源节点」或「目标节点」列的名称，展开提案详情卡片
- 详情卡片展示：关系类型（通常为「疑似重复」）、置信度分数（0-1，越接近1越相似）、判定依据

**状态筛选**
- 使用状态下拉框过滤：「待审批」「已通过」「已拒绝」

### 实际场景示例

> 系统在两次导入中分别创建了「张伟」和「张 薇」（名字含空格）两条人员记录。扫描后生成候选提案，置信度0.85。审批者点击来源节点名称展开详情卡片，对比两人的部门、工号、邮箱字段，确认「张 薇」是「张伟」的录入错误（多打了空格且性别字选错），点击「通过」。系统自动将「张 薇」的3条贡献记录和2个攻关单关联迁移到「张伟」名下，「张 薇」记录被删除。

### 注意事项
- **通过操作不可逆！** 合并后被删除的人员无法恢复，请务必仔细核对字段信息后再操作
- 建议在通过前检查两个节点的关联边数量和贡献记录，确保合并方向正确
- 置信度低于0.7的提案可能是误报，建议仔细核实

### 快捷操作
- 完成审批后再次点击「扫描候选」可发现新批次，建议每次数据导入后执行一次扫描`,
  },

  reminders: {
    title: "跟催提醒 - 使用帮助",
    content: `## 跟催提醒

> **菜单位置**：系统管理 → 审核管理 → 跟催提醒（**仅管理员可见可访问**；非 admin 直接访问 URL 会被路由守卫跳回首页）

### 功能概述
跟催提醒基于规则引擎自动扫描攻关单状态，识别需要关注的事项并生成提醒。支持三种提醒类型：问题单跟催、FE Deadline 提醒、CCB 提醒。提醒可通过邮件发送给相关人员，也可标记忽略。

### 操作指南

**生成提醒**
1. 点击页面右上角「扫描提醒」按钮触发规则引擎
2. 系统按以下规则自动检测并生成提醒：
   - **问题单跟催**：状态为「待响应」「处理中」的攻关单，且最近3天内无任何进展记录
   - **FE Deadline 提醒**：攻关单的 FE 截止日期距今不足2天
   - **CCB 提醒**：攻关单标记为需要 CCB 参与但尚未处理
3. 新生成的提醒以「待发送」状态出现在表格中

**处理提醒**
- **发送**：点击操作列的「发送」按钮，系统通过 SMTP 邮件将提醒发送给收件人，状态变为「已发送」
- **忽略**：点击「忽略」按钮，将该提醒标记为已忽略，状态变为「已忽略」

**查看详情**
- 点击表格中「主题」列的链接或操作列的「查看」，打开详情抽屉
- 抽屉展示完整信息：提醒类型、收件人姓名和邮箱、邮件主题、邮件正文内容

**状态筛选**
- 使用状态下拉框过滤：「待发送」「已发送」「已忽略」

### 实际场景示例

> 周一早上，指挥者点击「扫描提醒」，系统检测到攻关单「生产环境内存泄漏」已连续5天无进展（状态仍为「处理中」），生成一条「问题单跟催」类型提醒，收件人为处理人赵六。指挥者点击「发送」，赵六收到邮件：「您负责的攻关单"生产环境内存泄漏"已5天无进展更新，请及时同步进展。」赵六随即追加一条进展记录。

### 注意事项
- 发送提醒前需确保「邮件设置」页面已配置 SMTP，否则邮件无法发出
- 同一攻关单不会重复生成相同类型的待处理提醒
- 「忽略」操作仅对当前提醒生效，下次扫描如果条件仍然满足会重新生成

### 快捷操作
- 建议每天定时（如早会前）点击「扫描提醒」检查是否有新的待处理事项
- 批量处理时可先筛选「待发送」状态，逐条审核后发送或忽略`,
  },

  dailyReport: {
    title: "攻关日报 - 使用帮助",
    content: `## 攻关日报

### 功能概述
攻关日报自动汇总选定日期的所有攻关进展，聚合当日有更新的攻关单、进展条目数和状态分布，形成结构化日报。支持一键复制纯文本内容用于粘贴到即时通讯工具，也可正式发布为系统内日报记录。

### 操作指南

**日期导航**
- 点击日期显示区左侧的 **← 箭头** 查看前一天日报
- 点击 **→ 箭头** 查看后一天日报
- 点击「今天」按钮快速回到当天
- 点击日期文字打开日期选择器，可直接跳转到任意日期

**查看日报内容**
- 页面顶部展示汇总统计：当日触达的攻关单总数、新增进展条目数
- 状态分布区展示各状态（待响应/处理中/已解决/已关闭）的攻关单数量
- 下方按攻关单分组展示卡片，每张卡片包含：
  - 攻关单标题（可点击跳转到详情页）
  - 当前状态标签
  - 当日新增的进展条目列表（内容、时间）

**操作按钮**
- **复制**：点击「复制」按钮，将当日日报内容以纯文本格式复制到系统剪贴板，可直接粘贴到企业微信、钉钉等工具中
- **发布日报**：点击「发布日报」按钮，将日报正式发布为系统记录，发布后的日报可在攻关单详情的「日报更新」标签中查看
- **点击攻关单标题**：跳转到该攻关单的详情页，查看完整信息

### 实际场景示例

> 周五下午5点，指挥者需要向上级汇报当日攻关进展。打开攻关日报页面，日期默认为今天。页面显示：当日触达8个攻关单，新增12条进展。状态分布为处理中5个、已解决2个、待响应1个。逐一查看卡片内容后，点击「复制」按钮，系统将格式化的纯文本日报复制到剪贴板，指挥者粘贴到部门工作群中发送。

### 注意事项
- 日报数据基于进展记录的时间戳自动聚合，如果当天没有进展则显示空状态
- 已发布和未发布的日报不会重复，「发布日报」仅在当天尚未发布时可用
- 复制的内容为纯文本格式，不含表格和样式

### 快捷操作
- 在日期选择器中可以快速跳转到本周任意一天，便于回顾周报素材
- 点击攻关单卡片中的标题可直达详情页，方便在发送日报前核实具体进展`,
  },

  relatedPage: {
    title: "关联全景 - 使用帮助",
    content: `## 关联全景

### 功能概述
关联全景页展示指定节点的完整关联网络，包括直接的引用关系、跨颗粒度的间接关联、待审批的候选关系以及多跳扩展节点。通过调整探索深度，可以从单一节点出发逐层展开，快速发现隐藏的数据关联和冲突。

### 操作指南

**进入页面**
- 从攻关单详情页点击「关联全景」按钮进入，URL 格式为 \`/related/:nodeType/:id\`
- 页面顶部显示当前节点的类型和名称，左侧有返回按钮可回到上一页

**探索深度**
- 页面提供深度选择下拉框，可选 **1**、**2**、**3** 三个层级
- 深度 1：仅展示与当前节点直接关联的节点
- 深度 2：展示二跳范围内的关联节点（关联的关联）
- 深度 3：展示三跳范围内的关联节点，数据量较大，适合深入排查

**关联分类展示**
页面按以下类别分组展示关联节点：
- **出边（Outgoing）**：从当前节点发出的关系边，如攻关单→被分配的人员
- **入边（Incoming）**：指向当前节点的关系边，如贡献→关联的攻关单
- **同锚点（Co-anchored）**：通过共享锚点（如相同的问题单号）间接关联的节点，体现跨颗粒度关系
- **候选（Candidates）**：待审批的候选关联提案
- **扩展（Expanded）**：深度大于1的多跳关联节点
- **冲突（Conflicts）**：系统检测到的数据冲突项

**节点交互**
- 点击任意关联节点的名称链接，跳转到该节点的详情页或其关联全景页
- 每条关联显示关系类型（如 ASSIGNED_TO、CONTRIBUTED_TO、REF 等）

### 实际场景示例

> 指挥者发现攻关单「支付接口超时」可能与多个问题有关联。进入关联全景页，深度设为2。出边区域展示该攻关单分配给的处理人（REF边），同锚点区域展示另外3个共享相同锚点「PAY-SERVICE」的攻关单——其中「订单服务响应慢」状态为「已解决」，指挥者点击查看其解决方案作为参考。扩展区域展示这些关联攻关单又被其他贡献记录引用，形成完整的关联图谱。

### 注意事项
- 深度设为3时可能加载大量数据，建议先用深度1或2概览，再按需加深
- 同锚点关联基于配置中的锚点字段（如问题单号、产品模块），需在 Schema 中正确配置
- 关联全景仅展示结构化数据中的关系，不包含知识图谱的推导关系

### 快捷操作
- 利用浏览器标签页：右键点击关联节点名称在新标签页打开，可同时查看多个关联全景
- 通过返回按钮或浏览器后退键快速回到上一个上下文`,
  },

  mergePage: {
    title: "人员合并 - 使用帮助",
    content: `## 人员合并

> **菜单位置**：系统管理 → 人员合并（**仅管理员可见可访问**；非 admin 直接访问 URL 会被路由守卫跳回首页）

### 功能概述
将重复的人员记录合并为一条，用于数据清洗和实体统一。这是高敏感操作（不可逆），所以收紧到仅管理员可执行。

### 操作步骤
1. **选择被合并人员**（消失方）：从下拉列表搜索选择
2. **选择保留人员**（规范方）：从下拉列表搜索选择
3. **预览合并**：查看合并后的字段变化和关系迁移情况
4. **执行合并**：确认后执行

### 注意事项
> ⚠️ **此操作不可逆！**
> - 被合并方的所有字段将合并到保留方
> - 被合并方的所有关系边迁移到保留方
> - 被合并方将被永久删除
> - 请务必确认选择正确后再执行`,
  },

  importExport: {
    title: "数据导入/导出 - 使用帮助",
    content: `## 数据导入/导出

### 功能概述
通过 Excel 文件批量导入和导出各类数据,并支持**灵活容错**:遇到未匹配的列可在预览阶段勾选「自动建字段」一键创建后继续导入,无需手工到表结构管理添加。

### 导出数据
1. 选择要导出的数据类型(下拉切换)
2. 点击「导出当前数据」
3. 系统生成 Excel 文件并下载,列名采用 schema 里的中文 label

### 导入数据(标准模式)
1. 选择要导入的数据类型
2. 将 Excel 文件**拖拽**到上传区域(或点击选择文件)
3. 系统**预览**导入内容:
   - 将创建/更新/跳过的数量统计
   - **未匹配列**列表(本表 schema 中没有的列名)
4. 如需保留未匹配列,勾选「自动建字段」,系统按 Excel 中文表头自动追加字段到 schema
5. 确认后点击「确认导入」,后端开始批量写入并返回结果

### 导入规则
- 根据**身份键**(identityKeys)判断是新建还是更新
  - 攻关单: \`攻关单号\`
  - 人员: \`工号\` / \`邮箱\`
  - emailGroup: \`组名\`
  - infoCard: \`标题\`
- 身份键匹配到已有记录 → 更新合并(properties 浅合并)
- 未匹配 → 新建
- 验证失败的行自动跳过,统计列出原因

### 灵活导入(自动建字段)
当 Excel 表头里出现 schema 没定义的列(比如客户临时加的"现网影响半径"):
- 预览阶段会列出这些"未匹配列"
- 勾选要保留的列 + 「自动建字段」复选框,后端会:
  1. 把该列名作为 \`field.name\` 追加到对应 schema 文件
  2. 默认类型 \`string\`,label 同 name
  3. **导入完成后**,新字段立即出现在攻关详情/创建抽屉的「其它字段」分组里,无需重启

### 注意事项
- 灵活导入是**有副作用**的操作(改 schema 文件 + git 可追溯),建议确认列名拼写无误再勾选
- 表结构管理 (SchemaWizard) 仍然推荐作为主流建字段入口;灵活导入更适合一次性数据吸收场景
- 导出/导入对私密攻关单同样生效(导出时含 \`私密=是\` 标记,导入可恢复)`,
  },

  emailSettings: {
    title: "邮件设置 - 使用帮助",
    content: `## 邮件设置

### 功能概述
配置系统邮件发送服务（SMTP），用于求助中心发送邮件。

### 配置项
- **SMTP 服务器**：邮件服务商的 SMTP 地址
- **端口**：SMTP 端口号（通常 465 或 587）
- **用户名**：发件邮箱账号
- **密码**：邮箱密码或授权码
- **发件人**：显示的发件人地址

### 操作
1. 填写 SMTP 配置信息
2. 点击「保存配置」
3. 在测试区域输入收件人邮箱
4. 点击「发送」测试邮件是否正常`,
  },

  auditLog: {
    title: "审计日志 - 使用帮助",
    content: `## 审计日志

### 功能概述
记录系统中所有写操作的完整审计轨迹，用于追溯和合规。本页是「合规追溯」入口的最终着陆点。

### 筛选方式
- **操作类型**：创建/更新/删除/进展/设置/升级/合并
- **实体类型**：节点/关系/表结构/设置/提案/提醒
- **URL ?entityId=** — 从攻关单详情「合规追溯」卡跳转过来时自动加上，**一键锁定该节点全部审计**；上方会显示蓝色 Tag「已过滤实体: <名称>」，点 × 可清除

### 日志字段
| 字段 | 说明 |
|------|------|
| 时间 | 操作发生的时间（短格式，悬浮显示完整时间戳）|
| 操作人 | 执行操作的用户名 |
| 操作 | 创建/更新/删除/进展/设置/升级/合并（彩色 Tag）|
| 实体类型 | 节点/关系/表结构/设置/提案/提醒 |
| 实体 | **显示语义化名称**（标题/姓名等），不再暴露内部 UUID；已删除的显示 \`(已删除)\` |
| 变更详情 | 具体变更字段：旧值 → 新值（标签形式）|

### 实际场景示例
> 攻关单运行 3 周后，PM 想看看到底有过哪些关键变更。从详情页右侧勾选「合规追溯」侧边卡，点击「查看完整历史 →」，自动跳到审计日志页，URL 带上 \`?entityId=xxx\`，页面只列出与该攻关单相关的记录，从「创建」一路追溯到最近的「成员变更」和「升级」事件。

### 操作
- 点击「刷新」重新加载日志
- 点击「已过滤实体」蓝色 Tag 的 × 清除过滤回到全局视图`,
  },

  schemaWizard: {
    title: "表结构管理 - 使用帮助",
    content: `## 表结构管理

### 功能概述
管理系统的数据表结构（Schema），支持查看、创建和删除数据表。

### 左侧：现有数据表
- 展示所有已定义的数据表列表
- 点击表名在下方展开「字段详情」卡片
- 可删除不需要的数据表

### 字段详情卡片（选中表后展开）
- **新增字段**：填写字段名/显示名/类型（枚举类型再填枚举值），点「新增字段」即可。新增后，攻关单等页面的表单会自动显示该字段，无需到其它页面单独添加
- **停用/恢复**：停用后界面不再显示该字段（已有数据保留），可随时恢复
- 字段名在同一张表内必须唯一（它是数据存储与表单的键），重复会被拒绝

### 右侧：新建数据表
1. **表名**：英文 camelCase 标识（如 workOrder）
2. **中文显示名**：用于界面展示的名称
3. **字段定义**：
   - 字段名：英文标识
   - 标签：中文显示名
   - 类型：文本/数字/日期/枚举/引用等
   - 引用目标表（仅引用类型需要）
   - 枚举值（仅枚举类型需要）
4. 点击「添加字段」增加更多字段
5. 点击「创建数据表」提交

### 查找现有字段
- 点击「查找现有字段」按钮
- 根据当前字段名搜索已有表中的相似字段
- 可一键复用已有字段的概念和锚点配置

### v2.6: 字段分组管理(Schema as UI)
- **字段分组面板**:选中一张表后,详情卡顶部出现「字段分组」面板,列出当前 schema 所有分组 + 字段数;旁边的「新分组名」输入 + 「新建分组」按钮把第一个未分组字段作为占位移入新组(分组语义是字段属性派生,没有"裸创建分组")
- **行内分组切换**:字段表格新增「分组」列,行内下拉切换字段所属分组 → 后端 PATCH /api/schema/<nt> 走 op=updateField → 立即生效到详情页
- **顺序调整**:字段表格新增「顺序」列,显示当前 order 数字 + ↑ / ↓ 按钮,点击与组内上/下邻居交换 order(后端 updateField order 写回)
- **新增字段自动归组**:「添加新字段」面板加「分组(可选)」Select,选好分组直接落到指定位置
- **可见性条件**:FieldSchema 支持 visible 表达式(简易 eq/ne/in DSL,绝不 eval),如 \`状态 != "已关闭"\`,详情页基础信息 Tab 按表达式动态过滤显示字段
- **校验规则**:FieldSchema 支持 validation:{ pattern, min, max, minLength, maxLength },自动翻译为 AntD Form rules
- **默认值**:FieldSchema 支持 defaultValue(任意类型),用作新建表单的 initialValues

> 完整字段语义见 docs/SCHEMA_AS_UI.md。`,
  },

  configCenter: {
    title: "配置中心 - 使用帮助",
    content: `## 配置中心

### 功能概述
管理系统的下拉选项配置项，实现数据驱动的前端选项渲染。

### 主要功能

**新增配置**
1. 点击「新增配置」按钮
2. 填写配置键（如「状态」「事件级别」）
3. 填写显示名（可选）
4. 输入可选值（逗号或换行分隔）
5. 点击「保存」

**编辑配置**
- 点击行尾「编辑」修改可选值和显示名

**删除配置**
- 点击行尾「删除」确认后删除

**搜索**
- 输入关键词搜索配置项

### 配置覆盖机制
- 全局配置：键名为「状态」
- 页面级覆盖：键名为「attackTicket.状态」（仅影响特定页面）
- 前端加载时先查页面级配置，未命中则回退到全局配置

### 内置配置项一览（前端会自动消费）
- **攻关单状态/事件级别**：攻关单列表/详情/筛选下拉
- **贡献类型/贡献等级**：个人/团队贡献录入下拉
- **求助分类/求助状态/求助中心状态**：找帮手/求助中心
- **日报类型**：攻关日报抽屉
- **信息分类/重要程度**：信息广场
- **Bug 状态/Bug 严重程度**：问题反馈 + 截图反馈悬浮按钮
- **提案状态**：关系审批筛选
- **提醒状态**：跟催提醒筛选
- **团队角色**：攻关成员管理（组长/组员）

### UI 配置化原则（开发者注意）
- 所有业务枚举下拉必须用 \`useSettings().getValues(key, fallback)\` 读取
- fallback 是必填的 string[]，写上当前默认值，配置中心被清空 / 离线时 UI 仍可用
- 新增枚举 → \`scripts/settings-seed.mjs\` 同步 seed`,
  },

  notifications: {
    title: "通知中心 - 使用帮助",
    content: `## 通知中心 (Inbox)

### 功能概述
集中展示与当前用户相关的所有系统通知:攻关单逾期升级、跟催提醒、求助回复、问题反馈状态变更等。顶栏「🔔 铃铛」按钮显示未读数,点击展开最近 10 条;完整列表在「通知中心」页面 (\`/notifications\`)。

### 通知类型

| 类型 | 触发条件 | 收件人 |
|------|---------|--------|
| **升级 (escalation)** | 攻关单超过事件级别对应的 SLA(P1=2h/P2=8h/P3=24h/P4A=4h),未被处理 | 当前处理人 + 创建人 |
| **跟催 (reminder)** | 规则引擎扫描出待跟催的问题单 / FE Deadline / CCB | 提醒目标人(收件人姓名) |
| **求助 (help_request)** | 求助邮件发送失败 / 对方已回复 | 求助发起人 |
| **问题更新 (bug_update)** | 「问题反馈」状态变更 (待处理 → 处理中 → 已解决 / 已关闭) | 问题提报人 |
| **提及 (mention)** | 进展记录中提及 (@) 某人(后续版本接入) | 被 @ 用户 |
| **系统 (system)** | 平台公告 / 版本更新 / 维护通知 | 全员或指定群体 |

### 顶栏铃铛

- **位置**:全站顶栏右上角,用户菜单左侧
- **未读徽标**:红色数字气泡显示未读总数,最大显示 99+
- **实时推送**:浏览器接入 SSE (\`/api/notifications/stream\`),新通知秒级到达;断线自动回落到 30s 轮询
- **快捷弹层**:点击铃铛打开 360px 宽下拉面板,显示最近 10 条;点击单条标已读并跳转到 \`link\`;点击「查看全部」跳 \`/notifications\`
- **全部已读**:面板右上角「全部已读」按钮一次性把当前用户所有未读置已读

### 通知中心页面

进入 \`/notifications\` 看完整列表:

- **顶部按钮**:「刷新」「全部标已读」
- **筛选**:按类型(下拉)/ 已读未读 / 时间排序
- **列**:状态 / 类型 / 标题 / 内容 / 时间(相对时间 + Tooltip 完整时间)
- **行为**:点击标题跳到 \`link\` 并标已读;未读行带蓝底高亮 + 加粗

### 触发源接入(给开发者)

通知由后端**事件驱动**自动产生,业务代码不直接 import \`api\`,而是调用以下入口:

| 入口 | 触发位置 | 说明 |
|------|---------|------|
| \`scanEscalation(repo, notifications)\` | \`apps/backend/src/escalation.ts\` | 逾期扫描 → 自动给 owner + creator 推 escalation |
| \`scanAndCreateReminders(repo, registry, notifications)\` | \`apps/backend/src/reminders.ts\` | 规则扫描 → 推 reminder |
| \`makeHelpRequestRouter(..., notifications)\` | \`apps/backend/src/help-request.ts\` | 邮件失败 / 反馈到达 → 推 help_request |
| \`makeBugReportRouter(adapter, notifications)\` | \`apps/backend/src/bug-report.ts\` | bug status 变更 → 推 bug_update |
| \`POST /api/notifications\` (admin) | \`apps/backend/src/notifications-router.ts\` | 手动创建 / 系统公告 / 测试 |

### CLI

\`\`\`
npm run cli -- notifications:list [--unread] [--limit N]
npm run cli -- notifications:read <id>
npm run cli -- notifications:read-all
npm run cli -- notifications:create --user <u> --kind <k> --title <t> [--body <b>] [--link <url>]
\`\`\`

### 注意事项
- 通知与「跟催提醒」(reminder 决策队列) **是两个独立系统**:跟催提醒是 admin 操作台,逐条人工决定发不发邮件;通知中心是每位用户的私人收件箱。一条「跟催提醒」生成时会**同时**给收件人投递一条「跟催 (reminder)」通知。
- 当前后端**单进程内**通过内存订阅做 SSE fanout;集群部署需替换为 Redis pub/sub(已留扩展点)。
- 通知不入审计日志(audit_log) — 它是辅助渠道,真实业务事件本身仍在审计表里有记录。`,
  },

  bugReport: {
    title: "问题反馈 - 使用帮助",
    content: `## 问题反馈

### 功能概述
收集和管理系统使用中发现的问题、Bug和建议，支持截图和 Console 日志自动捕获。**两个入口都会写入同一列表**：本页「提交问题」+ 全站底部右下角「📷 截图反馈」悬浮按钮。

### 两种提交入口

**① 页内「提交问题」按钮**
1. 点击「提交问题」按钮
2. 填写问题表单：
   - **问题标题**（必填）：简要描述问题
   - **严重程度**：严重/较高/一般/建议
   - **问题描述**：详细描述问题现象、复现步骤、预期行为
   - **报告人**：您的姓名（可选）
   - **问题页面**：自动填充当前页面地址
   - **截图**：上传截图（支持拖拽 / Ctrl+V 粘贴 / 点击选择，最大 5MB）
   - **Console 日志**：系统自动捕获，也可手动粘贴
3. 点击「提交」

**② 全站悬浮「📷 截图反馈」按钮**
- 任意页面右下角的悬浮按钮（蓝色相机图标）
- 点击后自动 html2canvas 截当前页 + 记录页面 URL，弹出截图反馈抽屉
- 填写问题标题/严重程度/描述（截图已自动附上），提交即可
- 提交后本页若打开会**实时刷新出现新条目**，无需手动刷新

**状态流转**
- **待处理** → 开始处理 → **处理中**
- **处理中** → 标记已解决 → **已解决**
- **已解决** → 关闭问题 → **已关闭**

**筛选**
- 按状态筛选（待处理/处理中/已解决/已关闭）

**详情查看**
- 点击标题或「详情」查看完整信息
- 可查看截图、Console 日志、浏览器信息等

### Console 日志自动捕获
系统在页面加载时自动捕获浏览器 console 输出（log/warn/error/info/debug），提交问题时一并上传，便于问题诊断。`,
  },
  userManagement: {
    title: "用户管理 - 使用帮助",
    content: `## 用户管理

### 功能概述
用户管理页面允许管理员管理系统用户账号，包括创建、编辑、删除用户以及分配角色。

### 角色说明
- **管理员 (admin)**：可管理用户、配置系统、执行所有操作
- **Leader**：可标定贡献等级、审批关系、管理攻关单
- **普通成员 (normal)**：可查看数据和提交问题反馈

### 操作指南
1. 点击「新建用户」创建新账号
2. 在表格中点击「编辑」修改用户角色或重置密码
3. 点击「删除」移除用户（不能删除自己）

### 注意事项
- 系统初始化时自动创建默认管理员账号 admin/admin123
- 请在首次登录后及时修改默认密码`,
  },
  documentCenter: {
    title: "文档中心 - 使用帮助",
    content: `## 文档中心

### 功能概述
文档中心集中管理团队的参考资料与外部链接，支持上传本地文件和添加外链。每条文档都可一键复制链接或 Markdown 引用，便于在信息广场、动态标签等 Markdown 内容中插入引用。

### 操作指南

**上传文件**
1. 点击右上角「上传文档」按钮，选择本地文件
2. 上传成功后文档出现在列表中，类型显示为「文件」
3. 支持的文件随系统配置而定，上传后可下载查看

**添加外链**
1. 点击「添加链接」按钮，打开「添加外链文档」对话框
2. 填写文档名称和链接地址（URL）
3. 点击「添加」，该文档以「外链」类型出现在列表中

**每条文档的操作**
- **复制链接**：将文档的访问地址复制到剪贴板（纯文本 URL）
- **复制Markdown**：复制 Markdown 格式的引用（如 \`[名称](地址)\`），可直接粘贴到 Markdown 编辑器
- **下载 / 打开**：文件类型显示「下载」将文件保存到本地；外链类型显示「打开」在新标签页访问链接
- **删除**：弹出确认对话框，确认后移除该文档

**在 Markdown 中引用**
用「复制链接」或「复制Markdown」获取文档地址后，粘贴到信息广场（攻关详情的信息广场标签）或动态标签的 Markdown 内容中，即可在正文里引用文档或图片。

### 实际场景示例
> 攻关过程中产出一份《内核 panic 排查手册》，运维上传到文档中心，点击该条的「复制Markdown」，再到攻关详情的动态标签中粘贴，正文里就出现了可点击的手册链接，团队成员随时查阅。

### 注意事项
- 外链文档仅保存链接地址，链接失效时文档将无法访问
- 删除文档不会影响已粘贴到 Markdown 中的引用文本，但引用的地址会失效
- 「复制链接」得到纯地址，「复制Markdown」得到带名称的 Markdown 语法，按需选择

### 快捷操作
- 需要在文档中插图时，先上传图片到文档中心，再用「复制Markdown」粘贴到正文
- 外部资料（如官方文档、内部 wiki）用「添加链接」收纳，避免文件冗余`,
  },
  backupRestore: {
    title: "备份恢复 - 使用帮助",
    content: `## 数据库备份与恢复

### 功能概述
管理系统数据库的备份和恢复。支持手动备份、定时自动备份、下载备份文件、以及从备份文件恢复数据库。

### 操作指南

**立即备份**
点击页面右上角"立即备份"按钮，系统将创建当前数据库的完整快照。备份文件以 \`combat_backup_YYYYMMDD_HHmmss.db\` 格式命名。

**定时备份设置**
- 启用/关闭：通过开关控制是否自动备份
- 备份频率：支持每天、每周、每月
- 保留份数：自动清理超出数量的旧备份（默认保留 4 份）
- 系统每小时检查一次是否到达备份时间

**下载备份**
点击备份列表中的"下载"可将备份文件保存到本地。

**删除备份**
点击"删除"可移除不需要的备份文件。

**恢复数据库**
1. 点击"恢复数据库"按钮
2. 上传之前下载的 .db 备份文件
3. 系统将替换当前数据库并自动重启
4. ⚠️ 此操作不可撤销，请谨慎操作！`,
  },

  llmSettings: {
    title: "LLM 设置 - 使用帮助",
    content: `## LLM 设置（admin 专属）

v2.6 起，Hermes 智能问答所用的 LLM 全部由本页面在 UI 内配置，无需登录服务器改 env 或重启后端。**保存即生效**，下一次 \`hermes:ask\` 调用就用新配置。

### 字段说明

**Provider 提供商**
预设三个 provider，选中后会自动填默认 baseURL / model：
- \`zhipuai-coding-plan\`（智谱 AI）：baseURL = \`https://open.bigmodel.cn/api/paas/v4\`，defaultModel = \`glm-4.6\`，smallModel = \`glm-4.5-air\`
- \`huawei_cloud\`（华为云 ModelArts）：baseURL = \`https://api.modelarts-maas.com/openai/v1\`，defaultModel = \`glm-5\`
- \`custom\`（自定义 OpenAI 兼容）：自己填，凡是 OpenAI ChatCompletions 兼容协议（包括 vLLM、TGI、OneAPI 中转等）都可对接

**baseURL**
OpenAI 兼容的根路径。不要带 \`/chat/completions\` 后缀——后端会自动拼。改成自有反代/中转域名时直接覆盖即可。

**apiKey**
- 输入框 placeholder 显示当前已存的掩码（如 \`****cdef\`），表示 DB 内已有 key
- **留空 = 保留旧值，不修改**；只有粘贴新值才覆盖
- 后端用 **AES-256-GCM** 加密存储到 \`llm_settings\` 表的 \`api_key_encrypted\` 列
- 前端 GET 返回永远只含掩码，**不会返回明文**；浏览器、日志、审计均无明文泄露
- 仅 admin 角色可读写

**defaultModel / smallModel**
- defaultModel：所有 Hermes 问答主要走这个 model
- smallModel：留作未来轻量任务备用（当前未使用，安全填即可）

**thinking 思考模式**
- \`disabled\`：禁用 chain-of-thought（最快，适合简单查询）
- \`enabled\`：强制开启思考（更准，慢一些）
- \`auto\`：交由 provider 默认策略
- 调用时会在请求 body 里加 \`thinking: { type: 'disabled' | 'enabled' }\`；\`auto\` 时不发字段

**maxHops 工具最大轮数**
LLM 多轮工具调用的硬上限（1–12）。Hermes 一次问答中 LLM 调工具 → 看结果 → 再调工具，直到拿到最终答案或触顶。**建议 6** 已能覆盖 95% 场景；提高会拖慢响应。

**timeoutMs 单次超时**
单次 LLM 请求的硬超时（毫秒）。默认 60000（60s）。慢推理场景可调到 120000 以上。

### 操作流程

1. 选 provider，UI 自动填 baseURL / model 默认
2. 粘贴 apiKey（首次配置必填）
3. 点右上「测试连接」→ 直接打 \`POST {baseURL}/chat/completions\`，body = \`{ messages: [{role:'user',content:'ping'}], max_tokens: 16 }\`，成功会显示 latencyMs + modelEcho
4. 测试通过后点「保存」→ DB 写入 + 全站立即生效

### 常见 provider 速查表

| provider | baseURL | 主流 model |
|----------|---------|-----------|
| 智谱 AI | \`https://open.bigmodel.cn/api/paas/v4\` | \`glm-4.6\`、\`glm-4.5-air\` |
| 华为云 ModelArts | \`https://api.modelarts-maas.com/openai/v1\` | \`glm-5\` |
| OpenAI 官方 | \`https://api.openai.com/v1\` | \`gpt-4o\`、\`gpt-4o-mini\` |
| Anthropic via OneAPI | \`https://your-onapi.example.com/v1\` | \`claude-sonnet-4-5\` |
| 自部 vLLM | \`http://your-vllm:8000/v1\` | 自己的 model id |

### 安全模型

- apiKey 永远不写日志（请求 \`Authorization\` header 之外的任何地方都见不到明文）
- 数据库内 apiKey 用 AES-256-GCM 加密（key 由 \`COMBAT_CRYPTO_KEY\` env 派生）
- 前端 \`GET /api/llm-settings\` 响应剥离 apiKey，只留 \`apiKeyMasked\`
- 非 admin 用户访问 \`/llm-settings\` 路由会被 AdminGuard 重定向到首页
- 审计：每次保存写入 \`llm_settings.put\` 结构化日志（不含 key）

### CLI 等价命令

\`\`\`bash
npm run cli -- llm:get
npm run cli -- llm:set --provider zhipuai-coding-plan --base-url https://open.bigmodel.cn/api/paas/v4 --api-key sk-... --model glm-4.6 --thinking disabled
npm run cli -- llm:test --model glm-4.6
\`\`\`

agent 自动化部署、批量改 model 时直接走 CLI，避免人工 UI 操作。`,
  },
};

export default HELP;
